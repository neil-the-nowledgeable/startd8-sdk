# email-py (Step 3)

FastAPI HTTP port of `src/email/email_server.rb` with OpenFeature/flagd.

OpenAPI overlay: `api.yaml` (Role 2). Run `startd8 generate backend` in this directory
when wiring full deterministic contract emission.

**Reference:** Ruby Sinatra @ otel-demo 2.2.0
