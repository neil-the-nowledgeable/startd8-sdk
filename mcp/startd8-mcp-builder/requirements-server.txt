# Startd8 MCP Server Requirements

# MCP Framework
mcp>=1.0.0

# Data validation
pydantic>=2.0.0

# YAML parsing for SKILL.md frontmatter
pyyaml>=6.0.0

# Optional: For startd8_use_skill tool
anthropic>=0.18.0

# Optional: Prometheus metrics endpoint (STARTD8_MCP_METRICS_PORT)
prometheus-client>=0.20.0

# Optional: OpenTelemetry traces (STARTD8_MCP_TRACING=1)
opentelemetry-api>=1.30.0
opentelemetry-sdk>=1.30.0
opentelemetry-exporter-otlp-proto-http>=1.30.0

# Optional: For full Startd8 SDK integration
# startd8>=0.2.0
