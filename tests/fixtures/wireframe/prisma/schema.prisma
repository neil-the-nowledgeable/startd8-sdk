// Golden wireframe fixture (FR-W14 / R3-S3) — strtd8-shaped mini contract.
// Three concrete models + one enum + a relation; enables every entity surface.

model Profile {
  id        String   @id @default(cuid())
  name      String
  bio       String?
  metrics   Metric[]
  createdAt DateTime @default(now())
}

model Metric {
  id        String     @id @default(cuid())
  label     String
  value     Float
  kind      MetricKind @default(COUNT)
  profileId String
  profile   Profile    @relation(fields: [profileId], references: [id])
}

model Note {
  id    String @id @default(cuid())
  title String
  body  String
}

enum MetricKind {
  COUNT
  GAUGE
}
