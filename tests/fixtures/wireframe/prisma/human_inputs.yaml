# Owned-field policy: the AI edge must never write Metric.value — the canonical owned-field
# example (the reference consumer numbers this requirement FR-6; the policy is SDK-generic).
fields:
  - target: Metric.value
    authored_by: human
