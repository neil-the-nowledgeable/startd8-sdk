"""Capability Delivery Pipeline — Python orchestrator.

Replaces the 7-link bash chain with a single Python entry point.
Invokes the same CLI tools (contextcore, startd8) via subprocess.
"""

__version__ = "0.1.0"
