## Do this

1. Open `{{bundle_path}}` and follow it with filesystem write tools.
2. Write only the source paths listed below.
3. Write confirmation JSON to `{{status_writeback_path}}`.
4. Run `startd8 wloop run-next --job-id {{job_id}}`.
