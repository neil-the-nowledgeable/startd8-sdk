# Kickoff Portal — spike evidence

The throwaway spike is now the shipped `startd8 kickoff portal` command
(`src/startd8/kickoff_experience/portal_spec.py` + `tests/unit/test_kickoff_portal_spec.py`).
These PNGs are the rendered evidence retained from the spike:
- `kickoff-portal-canonical.png` — the canonical-`KickoffState` read slice (current).
- `kickoff-portal-snapshot.png` — the earlier provenance-heuristic render.
