# CRP Focus — Project-Start Distillation

## Least-reviewed target
`PROJECT_START_REQUIREMENTS.md` (v0.15) + `PROJECT_START_PLAN.md` (v1.1) — brand-new,
no prior external review. The Stakeholder-Panel sub-thread (FR-13/13a/13b/13c) was
pressure-tested via **8 live experiments** (documented in §0.2) but never externally
reviewed. The **plan (v1.1) is known-stale** relative to that panel work — flag where
it must catch up.

## SETTLED — do NOT relitigate (out of scope for suggestions)
1. The essential model: the SDK **defines the input contract and helps fill it
   honestly**; it does **not** host an interactive/agentic/multi-surface experience —
   the user's own agent (Claude Code/Cursor) drives.
2. The kernel = **one surface `startd8 kickoff`** = survey / instantiate / assess
   (greenfield) + `derive` as the on-surface **brownfield on-ramp** (OQ-9 RESOLVED —
   `derive` stays on-surface; do not re-argue moving it out).
3. `project init` **scoped OUT** to the VIPP / ground-truth-adjudication capability
   (OQ-8 RESOLVED — do not re-argue fold-vs-scope-out).
4. Welcome Mat / Red Carpet / Teian **retirement**.
5. The Stakeholder-Panel **reclassification** (discovery kept as a *facilitated*
   capability; point-value drafting dropped) and the empirical
   mirror→lens→trustworthy-convergence→decision-grade findings. The panel's *value
   case* is settled; its *engineering/rollout* is fair game.
6. The `ACCIDENTAL_COMPLEXITY_ANTI_PRINCIPLE` lens.

## Where we most need review (weight these)
1. **Phased-retirement + removal-criteria soundness (FR-9..12).** Are the deprecation
   markers, consumer-migration, and objective removal criteria sufficient and safe?
   Any ordering hazard (e.g. the `kickoff` name-collision sequencing constraint)?
2. **The real import-edge surgery.** FR-13/FR-15 (panel-in-`assess`: kernel-owned
   coverage core + opt-in-loaded persona layer; the `core.py:38-41` single-source
   preservation) and FR-14/FR-1a (`project init` VIPP de-coupling / scope-out). Are
   these cuts fully specified? Any residual coupling missed?
3. **The safe-write floor (FR-7)** and honest-provenance discipline (FR-8) — confined,
   human-privilege, no silent LLM writes; the MCP structural-read-only nit.
4. **FR-13c orchestrator-hardening completeness** — H1 artifact-grounding fidelity,
   H2 assumptions-as-gate, H3 cost tracking. Anything else needed before the panel is
   more than a prototype?
5. **Consumer-migration risk** — navig8 (kernel-only, VIPP-free, sole `derive`
   consumer) + the 2 VIPP apps (household-o11y, benchmark portal). Any break we missed?
6. **Requirements↔plan gaps/contradictions/over-reach.** The plan predates the panel
   experiments and OQ-8/OQ-9 resolutions — enumerate where it's stale or inconsistent
   with requirements v0.15.
