master craftsman workflow





it starts by deconstruct all the elements of the plan first with a low cost model, then with a high cost model to validate, and correct / update, using /agents when possible.  
It then IDs all applicable domains to check for lessons learned to inform design first with a lost cost model then high cost to validate, and correct / update , using /agents when possible.  
Then the worflow draft designs docs first with the low cost model then the high cost model to validate, and correct / update.  A second high cost models reviews and suggests improvements.  the original high cost model validates the suggestions and either applies or rejects them including the decision criteria used, using /agents when possible.
Next it builds tests first with a low cost model then a high cost model to validate, and correct / update, using /agents when possible.  
Then it starts development.
    It begins using a high cost model to decompose Code construction into logical more manageable chunks if needed to proactively avoid truncation and other generation issues related to large tasks.
    
    A high cost model then starts the iterative approach to building following an iterative approach.  First the any needed scaffolding, share infrastructure, system level elements etc. are built.  then the first real features will be added incrementally on top of that.

        When code construction begins start with a low cost model the components then use high cost model validate and integrate the drafted components, using /agents when possible. 
    
        Once this has been complete apply any available tests

        fix broken issues if any until none for this iteration of code development

        commit changes 
    
    check to ensure the repo is still clean before starting the next iteation of development 

    then start next iteration   

Once code creation is create user the high cost model for final assembly and validation of code integration

then perform Final Testing

Fix any remaining integration issues until none.  

Finally document difficulties or lessons learned details for later capturing 


__________________old_______________________________


master craftsman workflow

it starts by deconstruct all the elements of the plan first with a low cost model, then with a high cost model to validate, and correct / update, using /agents when possible.  
It then IDs all applicable domains to check for lessons learned to inform design first with a lost cost model then high cost to validate, and correct / update , using /agents when possible.  
Then the worflow draft designs docs first with the low cost model then the high cost model to validate, and correct / update.  A second high cost models reviews and suggests improvements.  the original high cost model validates the suggestions and either applies or rejects them including the decision criteria used, using /agents when possible.
Next it builds tests first with a low cost model then a high cost model to validate, and correct / update, using /agents when possible.  
Then it starts development.
    It begins using a high cost model to decompose Code construction into logical more manageable chunks if needed to proactively avoid truncation and other generation issues related to large tasks.
    
    A high cost model then starts the iterative approach to building following an iterative approach.  First the any needed scaffolding, share infrastructure, system level elements etc. are built.  then the first real features will be added incrementally on top of that.

        When code construction begins start with a low cost model the components then use high cost model validate and integrate the drafted components, using /agents when possible. 
    
        Once this has been complete apply any available tests

        fix broken issues if any until none for this iteration of code development

        commit changes 
    
    check to ensure the repo is still clean before starting the next iteation of development 

    then start next iteration   

Once code creation is create user the high cost model for final assembly and validation of code integration

then perform Final Testing

Fix any remaining integration issues until none.  

Finally document difficulties or lessons learned details for later capturing 






to validate, and correct / update.  
to validate, and correct / update.  
to validate, and correct / update.  
to validate, and correct / update.  



ID all applicable domains to check for lessons learned to inform design: 
    :lost cost then high cost

draft designs
    low cost model: 
    high cost model: validate and update

    double check design with high cost model 

builds tests
    low cost model: 
    high cost model: validate and update

Decompose Code construction into logical more manageable chunks if needed : High Cost Model
    use iterative approach, creating the scaffolding first then adding features incrementally

        low cost model to draft the scaffolding for first iteration features / components

        high cost model validate and integrate the drafted components 
    
        test against tests

        fix broken issues if any until none

        commit changes
    
    check to ensure the repo is still clean before starting again

    start next iteration.   

    code Complete

Final Assembly and validation

Final Testing

Document difficulties or lessons learned details for later capturing 


passes those to drafting agents to assemble code

claude assembles to code

code review

address issues from code review

test 

update

architecture code review

fix remaining issues

retest 

update 



low cost model:

identifies all applicable domains to check for lessons learned to inform design

high cost model: validate and update

draft designs
    low cost model: 
    high cost model: validate and update


builds tests
    low cost model: 
    high cost model: validate and update


draft designs
reviews lessons learned with haiku

passes those to drafting agents to assemble code

claude assembles to code

code review

address issues from code review

test 

update

architecture code review

fix remaining issues

retest 

update 


it starts by 
deconstruct plan elements 
    :lost cost then high cost

ID all applicable domains to check for lessons learned to inform design: 
    :lost cost then high cost

draft designs
    low cost model: 
    high cost model: validate and update

    double check design with high cost model 

builds tests
    low cost model: 
    high cost model: validate and update

Decompose Code construction into logical more manageable chunks if needed : High Cost Model
    use iterative approach, creating the scaffolding first then adding features incrementally

        low cost model to draft the scaffolding for first iteration features / components

        high cost model validate and integrate the drafted components 
    
        test against tests

        fix broken issues if any until none

        commit changes
    
    check to ensure the repo is still clean before starting again

    start next iteration.   

    code Complete

Final Assembly and validation

Final Testing

Document difficulties or lessons learned details for later capturing 


passes those to drafting agents to assemble code

claude assembles to code

code review

address issues from code review

test 

update

architecture code review

fix remaining issues

retest 

update 



low cost model:

identifies all applicable domains to check for lessons learned to inform design

high cost model: validate and update

draft designs
    low cost model: 
    high cost model: validate and update


builds tests
    low cost model: 
    high cost model: validate and update


draft designs
reviews lessons learned with haiku

passes those to drafting agents to assemble code

claude assembles to code

code review

address issues from code review

test 

update

architecture code review

fix remaining issues

retest 

update 
