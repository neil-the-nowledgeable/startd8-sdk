# CRP Focus — #229 grounded per-signal_kind thresholds (v0.3.1)

**Least-reviewed:** brand-new spec, zero prior review.

## Weight on:
1. **FR-2 the SRE-grounded values + FR-4 the behavior change (highest).** Filling saturation/run_success
   cells makes generate_functional_slos / #300-D2 / #308 BIND a graded SLO where they previously
   deferred/unfulfilled. Is that change correct and welcome (OQ-3)? Does it silently override the shipped
   NR-1 "no invented threshold" of D2/#308 — or is the config-table-as-grounding-seam reconciliation sound?
   Are the anchor values (0.8 saturation, 0.99 run_success) actually SRE-grounded, and does the monotonic
   ladder across criticality×mode hold (raising crit / installed→deployed never loosens)?
2. **FR-1 additive table + _select_importance_default keying.** Is passing signal_kind as field_name truly
   structure-free? A signal_kind cell absent for a (crit,mode) → None → defer: confirm that path.
3. **FR-3 grounding-pending marker (OQ-2).** omit-key vs explicit null vs marker — which keeps byte-identity
   + simplest resolution? Any way a grounding-pending cell accidentally resolves to a value?
4. **Unit correctness.** saturation=percentunit (0-1), run_success=ratio (0-1), freshness=s — a value in the
   wrong unit is the exact #229 category error re-introduced. Verify each filled cell's unit vs
   _FUNCTIONAL_SLI_TEMPLATES.
5. **NR-4 byte-identity.** A request-only (RED) service must resolve availability/latency/throughput EXACTLY
   as today — the new YAML keys must not perturb the existing cells or the flat-default fallthrough.

## Settled — do NOT relitigate:
- The shipped #226/#234 mechanism, #300/#307/#308.
- throughput stays flat (NR-2, existing FR-2b decision).
- The honesty discipline: freshness/queue_depth/lag/retry_rate are grounding-pending BY DESIGN (OQ-5);
  do NOT demand I invent their magnitudes.
- Values grounded in declared inputs + unit + SRE, not a faked live run.
