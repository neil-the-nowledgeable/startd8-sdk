# Session Ledger — Specs & Open Tasks (requirements-visualization + craft)

**Captured:** 2026-08-15 · **Refreshed:** 2026-08-19 (RECORD: `REQ-27-self-dogfood-verify-gate-adoption`
shipped `7e26e3ff`; open-queue #2 → done; Gate: adoption 9/9 mechanical) · **Scope:** the
requirements-visualization navigator work and the craft/design-principle promotions from this session.
Grouped by *state*, with the still-open items called out explicitly. Names follow the semantic-naming
convention (no integer+content-type alone).

> **Trust model (the standing caveat, now with a tool):** this "done" census is hand-maintained and DRIFTS —
> verify by FR-tag commits + tests, never by this list. **`scripts/verify_ledger.py` mechanizes that check**
> (dogfoods the navigator's own liveness/propagation principles): it flags any ✅ row whose cited commit sha is
> **UNLANDED** (not an ancestor of `main` — authored≠propagated) or whose artifact path is **PHANTOM** (absent on
> disk — presence≠liveness). Run it to trust the greens; hand-edit only the open queue below.
>
> **The verifier's own blind spot (found 2026-08-19): it audits only the ✅ rows.** A survivor census can't
> see a delivery that was never written down (REQ-26 shipped `c8745e5d` and stayed unrecorded — stage 6
> skipped) or a build-ready spec the open queue never enumerated (four of them). The done side reads clean
> *because* the drift migrated to the open side. Reconcile both directions: `verify_ledger.py` for the
> greens, and `navigator_spec_delivery_loop.py --status` cross-checked against this queue for the opens.

---

## ✅ Implemented (built + landed)

| Artifact | What | State |
|---|---|---|
| **REQ-01 SDK Node Home** | FR-1..FR-15 (added FR-11 structure-only, FR-12 debug panel, FR-13 provenance readout, FR-14 cruft purge, FR-15 scaffold mode); FR-1/4/6/8 gained `Name:`/`Touches:` | built, tests green |
| **REQ-02 N-level tree renderer** | `render_tree.py` ported from ContextCore live pair, XSS-safe, `--renderer tree` | **built, 235 tests** |
| **REQ-03 a11y renderer + corpus index** | `render_a11y.py` + `render_index.py` ported standalone; `build --format a11y` + `navigator index --dir` | **built, +20 tests** (`d416fc38`) |
| **REQ-04 lift lenses to shared transform** | `node_lenses.py` (`apply_node_lenses`/`apply_section_lenses`/`project_nodes`); compose is now a thin wrapper — byte-identity held | **built, 272 tests** (`5cb91c37`) |
| **REQ-05 graph/network-topology renderer** | `graph_projection.py` (ported) + `render_graph.py` standalone SVG; deterministic layout, cycle-safe; `--renderer graph` + `--semantic-only/--full-graph` | **built, 284 tests** (`b39540bd`) — 1st Spec Delivery Loop delivery |
| **REQ-09 shared-lens adoption** | tree + a11y inherit `node_lenses` via opt-in `--role`/`--fluency` (byte-identical default); `apply_node_lenses` wired 2/1 | **built, 302 tests** (`90efe69f`) — 2nd loop delivery; closes HTH dormant D-1 |
| **REQ-06 corpus governance** | `govern.py` + `navigator govern --dir` (5 checks, fail/advisory); consolidates the loop's stage-0 gate (Kagami); LOOP_CATALOG #7 | **built, 320 tests** (`54524784`) — 3rd loop delivery; 1st run flagged the seat-req drift |
| **REQ-07 diff-audience view** | `diff.py` (`diff_nodes → NodeDiff`, order-stable) + `render_diff.py` standalone delta renderer; `navigator diff --before --after` (+`--json`/`--max-detail`/`--role`) | **built, 367 tests** (`ecd96e02`) — 4th loop delivery |
| **REQ-10 View Definition + cascade (keystone)** | `view_definition.py` — serializable `ViewDefinition`/`ResolvedDefinition` (JSON round-trip) + per-leaf keyed-by-id cascade `resolve` + `BASE_NAVIG8R_DEFINITION`; requirements + capability domains re-expressed as `extends: base` + a thin delta, projecting to the existing `RenderProfile` (`to_render_profile`) — renderers unchanged, byte-identical. Keystone FR-5+FR-6: a base theme change propagates atomically to both domains while each keeps its overrides | **built, 400 tests** (`aa42795e`) — 5th Spec Delivery Loop delivery; architecture §7 step 1 |
| **REQ-11 Theme-token activation** (`feature/…-theme-ee3af56c`) | architecture §7 step 2 — the cascade's *visible teeth*. `RenderProfile.theme_tokens` (empty-default guard); `to_render_profile` projects the resolved `theme`; `render_html` emits a non-empty map as an additive `:root` CSS override (server-side splice before `</head>`, template constant untouched). FR-1 reconciled the base theme to the renderer's real `:root` values (byte-identity anchor). Proven end-to-end: capability renders `--accent:#3a6a94` vs requirements `--accent:#1b545f`; app path injects nothing | **built, 413 tests** (`a1a32eee`) — 6th Spec Delivery Loop delivery; byte-identity unedited |
| **REQ-12 Chrome-binding grammar** (`feature/…-chrome-from-22c6a41b`) | architecture §7 step 3 — `resolve_bindings` substitutes `{field}` placeholders; chrome carries a `bindings` map applied at projection ONLY under a per-doc context + only when fields resolve non-empty (else static). REQUIREMENTS_DEFINITION's FR-17/18 masthead rules are now declarative bindings; `requirements_profile_for` is a thin caller (compound page-title stays, NR-2). Existing FR-17/18 tests pass UNEDITED (the byte-identity oracle) | **built, 427 tests** (`d39e038f`) — 7th Spec Delivery Loop delivery |
| **REQ-13 Cross-repo VIEW-SCHEMA import** (`feature/…-externally-authored-d055cadd`) | architecture §7 step 7 (MECHANISM) — `load_definition` (JSON→ViewDefinition) + `resolve_external` (resolve an external def against the shipped base, registry never mutated) + `navigator view-definition --from <file>` (load→resolve→validate→dump). Synthetic 'legal' adopter fixture inherits the base theme + renders its own vocab/chrome. Real 2nd-repo onboarding is out of scope (NR-1 — the outward step this unblocks) | **built, 435 tests** (`fbd18f79`) — 8th Spec Delivery Loop delivery |
| **REQ-14 Control-region unification** (`feature/…-control-panel-93a3c2a4`) | architecture §7 steps 4+5 — lift the debug control panel + the scaffold region/layer taxonomy into the View Definition so the template READS them (closing the wired-vs-inert mirror the integration review flagged). 3 layers: model (`control` groups→toggles + `regions` id→layer+anatomy in the base, verbatim) → project (`RenderProfile.control`/`regions`, empty-default) → consume (`applyDefinitionOverride()` re-labels groups/toggles + re-sets `data-layer`/`data-scaffold` from the profile via data-group/data-region/id hooks). Reflective realization: FR-3/FR-5 as an **additive override** (REQ-11 theme pattern), because build-from-scratch would break FR-6's byte-identity + the unedited debug-panel test. Base ⇒ no-op ⇒ byte-identical; a domain delta overrides atomically; scaffold reveals the definition (FR-7). Supersedes the narrow `control-schema-formalization` | **built, 467 tests** (`a84bf12c`) — 10th Spec Delivery Loop delivery; `test_no_profile_is_byte_identical` + debug-panel test UNEDITED |
| **REQ-08 NL-Programming pipeline + Verify-oracle + provenance** (`feature/…-pipeline-stage-558d6fc7`) | the prose→product compiler made observable. `sources_pipeline.py` — 6 pipeline-stage Nodes via `category`+`attributes` (no `Node` field change), status from `sdk_artifact`-on-disk + a `pipeline` ViewDefinition (statuses keyed by NodeStatus ids). `verify_oracle.py` — promote `Verify:` to an acceptance oracle: classify (single-span extract, closed-set placeholder + multi-command guards) + opt-in `evaluate` (default-inert; `--run-oracle` runs only a read-only-`navigator`-subcommand allow-set, argv-token self-exec guard, `--oracle-timeout`, missing-input→`error`; **pass=rc0, prose assertion = human residue**). `pipeline_provenance()` sibling in `provenance.py` (longest-prefix ownership, not-found + SPEC-stage rows). `navigator verify` cmd + `--source pipeline`. Dogfood: `verify --run-oracle` ran FR-3's own clause → **pass**. Full reflective→CRP(R1, 17 accepted)→triage→loop arc | **built, 276 navigator tests** (`e870232c`) — 9th Spec Delivery Loop delivery; byte-identity + node_field_names goldens UNEDITED |
| **REQ-16 Node derivation edge + schema self-conformance · REQ-17 promote oracle/gate/history to Node fields** (co-delivered, one 0.4.0 bump) | the Node schema-version bump the handoff scoped as ONE `node_field_names()` golden delta. **REQ-17 (ADR impl):** `models.Node` gains `verify`/`approve`/`was` (optional, empty-default) — the acceptance oracle, human-approval gate, and change-history are now CARRIED, not dropped at the `det_req→Node` boundary; `sources_requirements` projects each FR's parsed values onto them (`attrs` kept as the render channel → byte-identity). **REQ-16:** new `DerivationEdge` (`from_key`+`relation`+reserved-**UNSET** `regime`, NR-6), distinct from containment `children`; `pipeline_provenance` now reads the chain from the typed edge (proven discriminating), not longest-prefix reconstruction. `NODE_FIELD_MANIFEST`+`field_parity_drift()` = the schema-as-Node self-conformance gate (`test_schema_conformance`, named-drift on un-manifested field; the 4 new fields register here, REQ-17 FR-3). `status_gap_class`/`health_gap_class` + portable SDK-import-free `fixtures/status_contract.json` (`test_status_agreement`) for the cross-repo twins. Golden delta = exactly `{verify, approve, was, derivation}`; `test_no_profile_is_byte_identical` UNEDITED; SDK-side only | **built + landed, 514 navigator/wireframe tests** (`aaa39178`) — 11th Spec Delivery Loop delivery; landed FF to reconciled `main` (`aaa39178`; the landing handoff was a transient process doc, never committed — a dangling ref `scripts/verify_ledger.py` flagged, now de-cited). **Follow-ons open:** cross-repo `dev-os/NODE-SCHEMA.md` §1 + CC mirror to 0.4.0 (authorized separate handoff); REQ-08 can read native `verify`; the realization REQ fills the reserved `regime` slot |
| **REQ-18 Realization regime + determinism-% rollup** (approach (a), (b)-ready seam) | fills REQ-16's reserved `regime` slot so the IR states HOW each node was realized + a determinism-%. **NO new Node field** (cleaner byte-identity than REQ-16/17) — `derive_realization` is a *function*, the facet *derived*. `realization.py`: the confidence-aware **seam** (`resolve_edge_regime`) is the (b)-ready **firewall** — absent/no-match/below-`CONFIDENCE_THRESHOLD`/non-declarable → degrade to *declared*, never assert (negative tests are load-bearing). `derive_realization` = regime **distribution** over the subtree (not min-rollup); `realization_facet` (incl. `mixed`) wired into the tree facet engine (`_facets_html`). FR-1: `DerivationEdge.regime` declared `deterministic` on pipeline stages (the `$0` compiler; bare edge still defaults `None`). FR-4: determinism-% summary line on `WireframePlan.realization` → appended to apex meta in `compose`, labeled **`declared`**. FR-5: invariant 9 govern check (`llm` edge ⇒ non-empty `verify`, activation-gated on `lives`), wired into `govern_corpus`. E2E: pipeline renders `5 deterministic / 0 llm / 0 human — 100% $0 (declared)`. Zero construction-subsystem imports (AST-checked) | **built + landed, 537 navigator/wireframe tests** (`e0be985d`) — 12th Spec Delivery Loop delivery; `node_field_names()` + `test_no_profile_is_byte_identical` UNEDITED. **Harvest backlog:** (H1) `format_determinism_line` counts a `mixed` node in the % denominator but not the visible counts string (cosmetic; can't occur in (a)); (H2) `_facets_html` calls `realization_facet` per-node → O(subtree) each → latent O(n²) on deep tree renders (fine at navigator scale); (H3) `govern`'s invariant-9 `except Exception: pass` swallows errors silently. **Next: REQ-19 (b)** fills the seam with measured construction provenance → determinism-% becomes `measured` (additive, no rewrite) |
| **REQ-19 Measured realization via a provenance contract** (approach (b)) | fills REQ-18's confidence seam with **measured** regime → determinism-% becomes `measured` not `declared`. **Two firewalls:** (1) a typed CONTRACT (`realization_contract.py`: `{file, regime, source_confidence, provenance{model,strategy,cost}}`; malformed → named `RealizationContractError`) is the SOLE coupling surface — the navigator imports only it, never a construction subsystem (AST-checked); (2) REQ-18's confidence seam (weak join → declared/`unknown`, never asserts). **NO new Node field.** FR-2: `backend_codegen/realization_emit.deterministic_records` + `micro_prime/realization_emit.llm_record` emit conforming records (importing the navigator contract — the firewall direction). FR-3: `realization_provenance.normalize` → one per-file map, conflicts resolved deterministically (confidence then fixed regime priority, never input order). FR-4: `MeasuredProvenanceSource` joins the map to `Node.lives` by path; `node_regime` now also grounds edge-LESS generated nodes via lives (threshold-gated). FR-5: `corpus_realization → (dist, grounded)`; above threshold relabels `measured`, below degrades. FR-6: `govern.check_determinism_regression` — planned-deterministic-but-measured-llm = named regression (SURFACES only). E2E: measured graph renders `1 deterministic / 1 llm / 0 human — 50% $0 (measured)` | **built + landed, 558 navigator/wireframe tests** (`bbf97542`) — 13th Spec Delivery Loop delivery; `test_no_profile_is_byte_identical` UNEDITED; the 5 `backend_codegen` golden failures are **pre-existing on main** (proven on pristine HEAD; my backend change is an additive unimported emit file). **Harvest backlog (the LIVE-path gap):** (H1) FR-2 emit helpers + FR-5/FR-6 CLI are **capabilities, not wired into a real run** — no `generate backend`/micro_prime run yet WRITES a provenance artifact, and no `navigator build/govern --realization-provenance` flag LOADS one → the measured path is library-tested but not yet reachable end-to-end from the CLI (the natural REQ-19 follow-on: wire emit into the generation flow + add the CLI flag); (H2) `corpus_realization` `grounded` is all-or-nothing — one measured node labels the whole % `measured` even if mostly declared (could be `measured (partial)`) | 
| **REQ-19 live-wiring** (harvest H1 follow-on, `b2dea22d`) | closes the measured path end-to-end: `generate backend --emit-realization-provenance` WRITES `realization-provenance.json` (sibling of app/, opt-in, byte-identical off); `navigator build --realization-provenance` LOADS it → renders `(measured)` (proven); `navigator govern --realization-provenance` activates FR-6 regression. 370 tests green | **built + landed** — the produce→consume loop now runs from the CLI |
| **REQ-15 Frame / scaffolding view** (5 FRs) | domain-neutral `navigator build --source frame` (FR-1, scaffold-on + content-hidden → region meta + control surface, free of any requirement); layer taxonomy reconciled into a keyed definition schema id→label/color/order (FR-2, fixes the 3-way legend inconsistency); legend + scaffold colouring rendered FROM the schema (FR-3); progressive per-layer disclosure (FR-4); additive + byte-identical (FR-5). Extends REQ-14. | **built + landed** (`c9bc0a3d` FR-1/3/4 · `7c583641` FR-2 · `ea65febf` #479 bare-frame); `test_view_definition.py` + `test_sources_and_cli.py` green (84 tests); `test_no_profile_is_byte_identical` UNEDITED. **Recorded 2026-08-17 — was misfiled in "Planned, no code yet" as "building now"; verify build status by FR-tag commits + tests, not the table** |
| **REQ-20 Lesson node + human-gated `revises` feedback edge** (retrospective bookend, increment 1) | the smallest proof the learning loop closes at the IR: a REQ-19 determinism-regression → a grounded `Lesson` node that `derives-from` it and carries a human-gated `revises` edge proposing a fix to the offending contract. Forward derived-from + backward revises = PDCA closed, visible + gated. **Propose, don't dispose — the IR NEVER autonomously modifies a contract.** **NO new Node field** (Lesson = `category="lesson"`+attributes, the REQ-08 pattern; `revises` = `EdgeRelation.REVISES` relation value on REQ-16's edge — no new structure). `build_lesson_from_regression` = the end-to-end proof; status `proposed→accepted/rejected` (default proposed), `revise_is_active` gates on `accepted`, rejected lessons RETAINED w/ rationale; **a negative test proves NO autonomous apply-revise path**. `govern.check_lesson_grounding` flags an ungrounded Lesson (cruft, inv. 4). FR-6: backward edges render through the EXISTING graph renderer — `derived-from` (cyan) vs `revises` (violet dashed) distinguished (also lights up the pipeline's REQ-16 edges). Live: `navigator retrospective --dir --realization-provenance` | **built + landed, 560 navigator/wireframe tests** (`8bf85ad2`) — 14th Spec Delivery Loop delivery; `node_field_names()` + `test_no_profile_is_byte_identical` UNEDITED. **Harvest backlog:** (H1) `nodes_from_retrospective` reads requirement nodes, which carry NO declared regime → a regression can't fire on them → the live source yields 0 lessons on a requirement corpus (fuel needs the planned regime on generated/pipeline nodes — the deeper follow-on, same class as REQ-19 fuel); (H2) `accept_lesson`/`reject_lesson` are library-only — no `navigator lesson accept/reject` CLI yet closes the disposition from the terminal; (H3) lessons are ephemeral (not persisted across runs — a "retained" rejected lesson is in-memory only). **Next: REQ-21** — the guarded, byte-identity-proven, reversible, audited auto-tier that amends NR-1 (default stays human) |
| **REQ-21 Guarded `revises` auto-tier** (byte-identity-gated, fail-safe, audited) | amends REQ-20 NR-1: a `revises` may auto-apply WITHOUT human accept **iff ALL** of — byte-identity-provable on the generated product ∧ reversible (git-tracked, no spend/outward-ship) ∧ above a grounding-confidence floor (`CONFIDENCE_FLOOR=0.8`); else `human`. **The gate is the OBJECTIVE byte-identity guard, not a "trivial" judgment** (which erodes — NR-2). `revise_tier.py`: FR-1 `classify_revise_tier` (the conjunction); **FR-2 enforce-not-declare** — `auto_apply_revise` applies THROUGH a byte-identity `guard` (fail-closed): a mis-classification whose application changes the product returns `None` (→ human), never shipped; FR-5 fail-safe — any UNRESOLVED (`None`) property → `human`; FR-6 `ReviseAudit` (lesson·target·guard-result·timestamp·revert_ref) + `govern.check_auto_revise_audit` flags an incomplete/irreversible record. Default stays human; every product-changing revise gated. `nodes_from_retrospective` tags each lesson's `revise_tier` (default `human` — byte-identity unproven at projection) | **built + landed, 569 navigator/wireframe tests** (`f6456c96`) — 15th Spec Delivery Loop delivery; `node_field_names()` + `test_no_profile_is_byte_identical` UNEDITED. **Harvest backlog (the applier seam):** (H1) `auto_apply_revise` is a SEAM — no live applier runs the byte-identity guard (regenerate+compare) over the real product yet, so the auto-tier is specced+tested but never fires live (default human everywhere; same class as REQ-19/20 seams); (H2) `build_lesson_from_regression` sets no `confidence` → a regression-lesson is `confidence=None` → below floor → **can never be auto-eligible even with a guard**; the confidence should carry from the regression's measured `source_confidence` (a wiring gap); (H3) `CONFIDENCE_FLOOR` is a hardcoded constant (could be config). **The realization→retrospective arc (REQ-16→21) is complete on `main`** |
| **Retrospective fuel** (REQ-20 H1 + REQ-21 H2 closed, `bc702d04`) | the realization→retrospective→auto-tier arc now **fires live end-to-end**, not just on fixtures. **H1:** `nodes_from_retrospective` also scans the PIPELINE source (declared regimes) → a provenance drift (a stage declared deterministic but MEASURED llm) fires a real determinism regression → a Lesson. **H2:** the Lesson carries the measured join **confidence** (`build_lesson_from_regression(confidence=…)` via `_measured_confidence`) → a high-confidence regression is above-floor → the REQ-21 auto-tier is **reachable** (was structurally impossible while confidence was None); a low-confidence one stays `human` (firewall carries through). E2E proof: drift → regression → grounded Lesson (conf 0.95) → auto-tier eligible → `auto_apply_revise` through a passing guard → an audit record | **built + landed, 571 navigator/wireframe tests** (`bc702d04`); ruff clean; byte-identity UNEDITED. **Remaining seam:** `auto_apply_revise` still needs a real construction-side byte-identity guard (regenerate+compare over the actual product) to auto-apply live — the deeper applier follow-on |
| **REQ-24 Byte-identity revise applier** (closes REQ-21's guard seam) | fills REQ-21's `auto_apply_revise(…, guard)` with a **real** byte-identity guard — the auto-tier now acts against the ACTUAL generated product, not a mock. Delivered via the **reflective-requirements loop**, which reshaped the increment twice: (1) **deleted** the "safe revise class" allowlist — the guard PROVES byte-identity per-revise (enforce-don't-declare); (2) reframed the fuel to a **concrete `ReviseEdit` payload** (the regression Lessons are consequential, NR-3); (3) **build-time discovery** — every generated file carries a `# schema-sha256:` provenance header, so strict byte-identity can't pass a schema edit → the guard compares **modulo the source-fingerprint stamp** (excludes provenance metadata, not behaviour; verified: a comment edit changes 0 real files, a rename changes 10). FR-2 `byte_identity_guard` REUSES `render_backend` (Mottainai); FR-3/4 `apply_revise` runs it THROUGH `auto_apply_revise`, writing the contract ONLY on proof+audit+`--apply` (dry-run default → downgrade leaves the tree byte-identical); FR-5 `navigator revise-apply`; FR-6 firewall — the applier imports `backend_codegen`, the navigator core stays construction-free (AST) | **built + landed, 403 navigator/wireframe tests** (`1a8b9b44`) — 16th delivery (1st via reflective-requirements); `node_field_names()` + `test_no_profile_is_byte_identical` UNEDITED. **Harvest backlog:** (H1) no Lesson→`ReviseEdit` producer yet — the CLI takes a human/tool-provided edit.json; a description-clarification lesson source is the follow-on; (H2) the guard is bound to `backend_codegen` (the Python `$0` path) — other output kinds/polyglot aren't covered; (H3) `--apply` writes the schema but doesn't git-commit (revert = `git checkout`, manual). **Meta-lesson:** the reflective loop's biggest catch (the sha256 header) was a build-time discovery — planning-by-exploring must inspect the OUTPUT shape, not just the API signature |
| **REQ-22 Verify-liveness (not presence)** | closes the presence≠liveness lacuna (the NetBSD `O-4` Functional Spine Fracture: a verify points at real code + passes every structural check while its guarantee is DEAD). Makes verification **live**, reusing shipped machinery (no new engine). FR-1: `verify_gate` — an optional runnable gate handle (Node field #20, +`Gate:` det-req parse). FR-2/3/4: `check_verify_liveness` reuses `verify_oracle._classify_clause` — a gate that doesn't resolve to a runnable command = STRUCTURAL death → **GAP** (fact/fail); resolves-but-missing-input = provenance **CANDIDATE** (advisory) — the absence-vs-error move; only realized nodes; no execution for the structural check. FR-5 `recheck_verify_liveness_on_drift` (impl-provenance change re-checks dependent gates). FR-6 `build_lesson_from_liveness_gap` (dead gate → proposed human-gated retrospective Lesson). **FR-7: invariant 9 STRENGTHENED presence→liveness** (a realized llm node with a present-but-dead verify now violates — amends REQ-18 FR-5). FR-8: NetBSD `O-4` = negative-control fixture → GAP; clean corpus flags 0; advisory (never blocks) | **built + landed, 591 navigator/wireframe tests** (`27399e2c`) — 17th delivery; `test_no_profile_is_byte_identical` UNEDITED; golden delta = exactly `{verify_gate}`. **Cross-repo follow-on (NR-5):** the `verify.gate` det-req-kit schema field (dev-os) + the CC Node mirror need the additive field — a coordinated handoff (like REQ-16/17). **Next: REQ-23** (liveness-layer fact-first cells) builds on this |
| **REQ-23 Liveness-layer fact-first cells** (target-unmeasured + served-by-a-dead-FR) | ships the two FACT cells of the present-but-dead requirement-level column (structural death → GAP, can't cry wolf); hypothesis cells deferred (NR-3). Reuses REQ-22 + the roll-up; **NO new Node field** (target/signal ride the objective node's attributes — objective-specific, no golden bump). FR-1: `outcome_nodes_from_requirements` projects each objective (O-N) as an `objective` Node with `target` + optional `target_signal` (a `Signal:` binding). FR-2: `check_target_unmeasured` — a target with NO bound live signal = structural GAP (the authoring-time twin of Feature-Observability's loud-gap-for-a-goal). FR-3: `check_served_by_dead_fr` — verify-liveness rolled up the serves-edge: an outcome ALL of whose realized serving FRs are dead = GAP; ≥1 live ⇒ clean (min-rolls-up). FR-4: both GAP + route to `build_lesson_from_liveness_gap` (human-gated). **FR-5: `check_liveness_layer`** — ONE `liveness` govern layer (verify-liveness + target-unmeasured + served-by-a-dead-FR), each finding ref-tagged `liveness:<cell>`, not a scatter. FR-6: advisory, byte-identical | **built + landed, 598 navigator/wireframe tests** (`821d9b17`) — 18th delivery; `node_field_names()` unchanged (20); `test_no_profile_is_byte_identical` UNEDITED. The present-but-dead **column is now enforced as one layer** |
| **Retrospective-loop seams** (REQ-20 H2/H3 + REQ-24 H1/H3 closed, `3527aa4e`) | harvest-backlog closure over the REQ-20/24 loop — four specced-but-unfueled seams, now wired live end-to-end. **REQ-20 H3:** new `lesson_store.py` persists Lessons across runs; `merge_lessons` refreshes derived content but **preserves the human's disposition** (accepted/rejected never resets to `proposed`; a judged Lesson outlives the run that proposed it — cross-run Kaizen memory). **REQ-20 H2:** `navigator lesson list\|accept\|reject` over the store — the ONLY disposition path, always persists. **REQ-24 H1:** `revise_edit_from_lesson` extracts the concrete edit a **description-clarification** Lesson carries (`build_lesson_from_description_clarification`); a determinism-regression Lesson carries none (its fix = plan re-examination) → `None`; `revise-apply` derives the edit from `--lesson` when `--edit` is omitted. Elegant closure: a prose clarification regenerates the `$0` product byte-identical (modulo the sha stamp) → exactly the auto-tier-eligible class. **REQ-24 H3:** opt-in `revise-apply --commit` captures the written contract as a real commit (stages only the schema file, never `-A`). | **built + landed, 408 navigator tests** (`3527aa4e`) — 9 new (`test_lesson_store.py`); `node_field_names()` + `test_no_profile_is_byte_identical` UNEDITED (no models/render change); ruff clean; live CliRunner-proven. **Follow-on REQ-24 H2 now also CLOSED** (`144761ca`, below); REQ-19 measured-provenance fuel was already closed by `b2dea22d`. |
| **REQ-24 H2 — revise guard across deterministic output kinds** (`144761ca`) | generalizes REQ-24's byte-identity guard from `render_backend`-only to a **pluggable regenerator registry** keyed by output kind: `revise-apply --kind backend\|scaffold` (extensible to views/frontend, both `(path,content)`-shaped). The polyglot boundary is made **honest, not covered**: a non-deterministic / LLM kind has no regenerator → the revise **fails safe to `human`** (byte-identity is unprovable for LLM output — REQ-21's own principle). Build-time discovery (same class as REQ-24's schema-sha256 catch): `_PROVENANCE_MARKERS` stripped only 3 of the 11 `$0` source-fingerprint stamps, so scaffold's `manifest-sha256` survived → scaffold could NEVER pass; fixed to strip the whole family. | **built + landed, 414 navigator tests** (6 new H2); byte-identity + `node_field_names()` + the FR-6 AST firewall UNEDITED; ruff clean; live-proven (scaffold auto-eligible, polyglot fail-safe) |
| **REQ-25 — liveness-layer hypothesis cells** (fact-rungs ship, judgment-rungs park, `2097d43e`) | completes the present-but-dead column (REQ-22/23 = fact cells) **without crying wolf**. Each hypothesis cell = a deterministic **fact-rung** (ships as GAP/trigger, REUSES an existing checker — NR-2) + a semantic **judgment-rung** (parked-by-default). **FR-1 mitigation-inert:** a node's declared `security_mitigation` the `query_prime/security` verifier reports ABSENT in its realized code → GAP. **FR-2 non-goal-violated:** a `wont` import-ban (`no-import:<mod>`) the code AST-imports → GAP. **FR-3 touches-provenance-changed:** a Touches'd file whose REQ-19 provenance moved → re-judge trigger. **FR-4/5/6 parking:** `JudgmentRung`/`is_unparked`/`run_judgment_rung` — parked by default (execute nothing); un-park needs precision ≥ `PRECISION_THRESHOLD` (REQ-07 FR-7) **AND** a verify-live LLM-judge (FR-6 dogfood — the checker can't trip its own class); un-parked → evidence-citing **candidate**, NEVER a GAP (NR-4). **FR-7:** registered in `check_liveness_layer` Tier-2; GAPs route to a proposed human-gated Lesson (`lessons_from_liveness_layer`, reuses REQ-20; candidates don't route). **FR-8:** the two fact-rungs wired into `govern_corpus` **opt-in** → byte-identical on the real corpus (0 findings, exit 0) yet reachable (no seam-fuel). | **built + landed, 427 navigator tests** (11 new `test_liveness_hypothesis_cells.py`); byte-identity goldens UNEDITED; ruff clean; live govern-proven. Build-time discovery: the reused `ast` walk recorded only `from X import Y`'s module, missing the banned name — fixed to record `X.Y`. **Judgment-rung parking machinery done; EXECUTION grounded by the fixture set below.** |
| **REQ-25 judgment-rung fixture set** (grounds the precision gate, `785fb317`) | authors the labeled fixture set FR-4 references + the harness that consumes it, so a rung's `precision` is MEASURED, not hand-passed. `tests/.../fixtures/liveness_judgment_fixtures.json` = **24 labeled semantic cases** (8/cell, 4 pos + 4 neg) — the RESIDUAL judgments the fact-rungs can't catch (mitigation present-but-inert; non-goal violated in spirit not by import ban; a changed Touches'd file that breaks-vs-preserves the claim). `govern.py`: `JudgmentFixture` + `load_judgment_fixtures` + `measure_precision` (TP/(TP+FP) — **precision not recall**, NR-4; undefined→parked) + `measured_judgment_rung` (measures over the cell's fixtures + derives `judge_verify_live`). `is_unparked` now gates on a MEASURED precision. | **built + landed, 443 navigator tests** (10 new); byte-identity goldens UNEDITED; ruff clean. Proven (mock judge, no LLM): accurate verify-live judge → un-parks + emits candidates; wolf-crier / one-FP (0.8) / non-verify-live → all stay parked. **Residual = plug a real LLM judge (optional).** |
| **REQ-26 a11y as a cross-topology lens** (the fourth RenderProfile moment) | lifts accessibility out of the flat-only renderer into ONE generic cross-topology projection (`a11y_view_of_nodes`) wrapped by ONE shared accessible shell (`_a11y_shell` — the no-fork chokepoint); tree/graph/diff each render an accessible semantic view through it, the flat requirement view stays byte-identical. Reflective correction v0.1→v0.2 folded into §0: FR-1 re-homed (a11y is a PRESENTATION/modality axis, NOT a `node_lenses` audience value — that would violate the lens module's import firewall; the two axes COMPOSE), FR-4 re-wired (`navigator diff --a11y`, not a `--renderer diff` value), FR-6 sharpened (the single generic projection IS the FF-1 closure — a new renderer with a `List[Node]` inherits a11y with zero a11y-specific code). Reuses the flat CSS/esc/status-word/`_safe_href` primitives; guarded by `check_no_bleed` | **built + landed, 693 navigator/wireframe tests** (`c8745e5d`) — 10 FR-keyed pins (`test_a11y_lens.py`); flat a11y + `test_no_profile_is_byte_identical` UNEDITED; ruff clean. **Recorded 2026-08-19 — stage 6 was SKIPPED at delivery time; found by a loop↔ledger reconciliation, not by the ledger itself** |
| **REQ-cross-surface-view-definition** | lifts node health to a shared cross-SURFACE owner: base `node_state` (canonical ids = navig8r keys verbatim — `grounded`/`spec`/`awaiting`/`excluded`/`unknown`; `activated` is the project-level roll-up only) + per-surface `presentation` (navig8r leaf = today's `vocabulary.statuses` Derive-to-Prove oracle; cockpit leaf declares `_ATTENTION_DISPLAY` keys) + `surface_links` (`drill` via registered `fullview` `#<key>` region; `rollup` via composition `serves`). `to_render_profile` reads `node_state.presentation.navig8r` when a domain's status keys **equal** that map (requirements); a proper subset is not an opt-in. Capability/pipeline/node-schema keep their vocabularies (empty-default). **No new Node field. Cockpit wiring OUT (NR-7)** — presentation is declared, `kickoff_experience/` has zero diff, so drill/rollup are not user-visible until a follow-on adopter spec. `_template.py` + `graph_projection.py` untouched | **built + landed, 73 FR+golden tests** (`67051859`; PREP lock `1940b8d8`) — 19th Spec Delivery Loop delivery; `test_no_profile_is_byte_identical` + view-definition goldens + `node_field_names()` UNEDITED; ruff clean; reachability: no new public symbols. **Harvest (H1):** cockpit-side adopter to READ `surface_links` and link tiles to `#<key>` — this delivery is definition-only |
| **REQ-27 Self-dogfood verify-gate adoption** | dogfoods REQ-22's remedy on our own corpus: classify every FR verify via `verify_oracle.classify` (command/assertion/manual); adopt `Gate:` on all mechanically-attestable (`command`) FRs; mark legitimately-manual with explicit `Manual:` (dogfooded on REQ-22's 8 ironic prose FRs); standing advisory self-gate (`check_self_dogfood_verify_gates` + `navigator_spec_delivery_loop.py --self-dogfood` / `--status` line) reports adoption + mechanical-but-gateless; gateless mechanical FRs route to a `proposed` Lesson (`build_lesson_from_mechanical_gateless`). **No new Node field** (`Manual:` → `attributes["verify_kind"]`). `Gate:`/`Manual:` parse made **case-sensitive** (REQ-04 FR-6 prose "gate: if it fails…" was a false-mechanical). Not a 6th REQ-06 charter check (advisory only, NR-2) | **built + landed, 19 FR pins in `test_self_liveness.py`** (`7e26e3ff`) — 20th Spec Delivery Loop delivery; adoption **9/9 mechanical (rate 1.0)** + 301 honest-manual (8 explicit); `test_no_profile_is_byte_identical` + `node_field_names()` (20) UNEDITED; ruff clean. **Unblocks (partial):** whole-liveness-layer govern wiring — `Gate:` adopted; still needs `Signal:` authoring before wiring `check_liveness_layer` into `govern_corpus` |
| **REQ-view-definition-mode + control consolidation** (13 FRs) | the in-render View picker + control-surface consolidation: FR-1 "View Definition" as an in-render pick of the current renderer; FR-2 collapse the control panel to ONE View picker + ONE additive Overlays stack (retiring the Structure/Combined density modes + scaffold-only duplicate; their per-node meta preserved as a Show-node-metadata overlay); FR-3 taxonomy/labels definition-owned; FR-4 one frame mechanism / two entry points; FR-6 the View Definition shows every region's display-logic template; FR-7 definition-aware frame provenance (no false cruft); FR-8 Debug group (raw data / node data); FR-9 definition-owned paging; FR-10 per-cell node inspector; FR-11 edit not-displayed fields live (non-persistent); FR-12 inline per-field display switch; FR-13 migrate the display-logic DATA into the View Definition | **built + landed (incrementally), 71 view/render tests** — feat commits per FR (`e1cdcca1` FR-6 · `0d73eff6` FR-7/8 · `614b0502` FR-9 · `66864a51`/`56e9a2c7` FR-10/11 · `18904ca3`/`95b76f00` FR-12 · `f2a09543` FR-13); FR-1/2 in `_template.py`+tests. **Recorded here 2026-08-16 (was mis-read as spec-only — built via per-FR commit messages, not one `REQ-view-definition-mode` commit; the stage-0 gate reads spec-parseability, NOT build status)** |
| **5 loops + catalog** | pilot / content / origin-audit / cruft / inspect loops + `docs/LOOP_CATALOG.md` | built |
| **Node-schema source (Kagami mirror)** | `sources_node_schema.py` introspects `Node` → Nodes | built |
| **Naming convention** | `naming.py` (`name_forms`), `docs/NAMING_CONVENTION.md`, det-req `Name:` field | built |
| **Universal principle promotion** | Mottainai + Personal Conway → craft canonical; SDK de-forked; `craft/design_principles/README.md` join index (closes Lacuna-1) | committed both repos, on main |
| **Meta-analysis corpus** | Craft Grammar, NL-Programming-System thesis, reflective-audit duality, 4 new reflective skills | authored |

### det-doc-kit family — the `$0` NLPS document-layer projectors (2026-08-17 arc; was ABSENT from this ledger until now)

| Artifact | What | State |
|---|---|---|
| **REQ-29 det-plan projector** (`plan_codegen/`) | the `$0` REQ→`det-plan/0.1` projector — the "generate plan" analog of "generate backend"; iterations/targetFiles/gate/costClass derived from a REQ's FRs/Touches/Verify, `dependsOn` from authored `Depends:` only (cycle-rejected via `queue.py`), solo-vs-gap gate, maturity 0.1, SARIF via imported `findings_sarif`; `startd8 generate plan` + registered deterministic provider. Piloted golden-first on 5 REQs → friction folded into `SCHEMA_det-plan-0.1` (G-1/G-2/G-3). Then full **harden-then-harvest**: `/code-review` (CLI tests + Depends robustness) → `/python-code-refactor` (observable provider) → `/reflective-retrospective` (extracted `STANDARD_det-doc-kit-projector-pattern.md`) → `/enhancement-backlog` | **built + landed, 36 tests** (`35e75c89` feat · `4d62711a`/`33606c12` HTH harden · `c3273dec` standard · `38a84720` backlog) |
| **det-crp format+lint kit** (`SCHEMA_det-crp-0.1.md`) | grounding found det-crp is **NOT a projector** (its `$0` generator, the `new-cnvrg-rvw-prmpt` compiler, already runs; the review-log is human-accreted) → a thin format+lint kit citing the compiler. Corrected the STANDARD's over-claim | **format + lint BUILT** (`SCHEMA` `8169bf50`; `crp_lint` — `src/startd8/crp_lint/` + `scripts/crp_lint.py`, 12 tests, dogfooded clean over 26 docs) |
| **det-handoff projector** (`handoff_codegen/`) | the real **2nd projector** — `$0` REQ+ledger→`det-handoff/0.1`; spine (spec/buildOrder←FRs/exitCriteria←Verify/prereq-audit/pointers/handBack←Objectives) derived, Gotchas+framing human-residue; `generate handoff --requirements/--ledger/--base`. The `/reflective-adoption` gate **PASSED** (0 new shape-gaps). Findings: shared `req_header` extracted (plan migrated too); solo-vs-gap signal is doc-type-specific (ledger state) | **built + landed** (`SCHEMA` `09e1c5ad` · projector `5ba7eb48` · adoption-close `55aac63f`) |
| **det-howto projector** (`howto_codegen/`) | the **3rd projector, built INDEPENDENTLY** by a fresh-context subagent from the STANDARD alone (never read the siblings) → discharged the single-agent honesty caveat; `$0` REQ→`det-howto/0.1` command-reference (a degenerate-edge projector: most of a HOWTO is human-residue). Surfaced 4 mechanism-gaps the single-agent builds couldn't → folded into STANDARD §I-1b / Part-5 exit-code table / Part-6d back-ref contract + `SCHEMA_det-howto §0.1/§2` | **built + landed, 23 tests** (`2baf4de0`); STANDARD status → **INDEPENDENTLY REPLICATED** |
| **shared `pairs_with_line` helper** (`req_header`) | metabolized the Part-6d finding: the render↔provider back-reference line was a two-file byte-contract the 3 projectors had drifted on → ONE shared `render_pairs_with_line`/`parse_pairs_with_line`; all 3 migrated (no local regex). 4 plan `.projected.md` artifacts stay byte-identical | **built + landed, 79 det-doc tests** (`72fdd740`) |

> **det-doc-kit result:** 3 working `$0` projectors (plan/handoff/howto) + 1 format+lint kit (crp) + a STANDARD proven by **independent** reuse. **NOT pushed to `origin`** (local `main` only, through `72fdd740`). **Open tails** hoisted to §Open follow-ups below.

---

## 📝 Planned, NOT explicitly deferred (open specs — no code yet)

> **Navigator REQ-01..27 arc: none.** (REQ-15 moved to Implemented on 2026-08-17 — it was misfiled here as
> "building now" but is built + landed `ea65febf`. The only remaining open work on that arc is the two seams
> below, both of which HAVE code and are extensions, not greenfield specs.)

### det-doc-kit metabolization arc (open specs, 2026-08-17) — the CRP-theme shift-left

> The CRP-theme metabolization thread (four parallel investigations → `SYNTHESIS_crp-theme-metabolization-four-investigations.md`)
> produced one spec + a named backlog. **Key finding for the builder: these are NOT greenfield — REQ-32's
> draft-surface wire is a fan-out of the already-built + fixture-proven REQ-25 fact-rung/judgment-rung
> machinery** (`2097d43e` + `785fb317`), and REQ-30/31 are the same class as the open `Depends:` grammar tail.
> The convergence: all four themes queue at ONE seam (REQ-32); wire it once → each theme is one additive predicate.

| Spec | What | State | Effort | Closest relative — REUSE, don't rebuild |
|---|---|---|--------|------------------------------------------|
| **REQ-32 draft-time firing wire** | the convergence unlock — inject settled-themes into the CRP prompt (FR-1) + fire fact-rung theme lints in det-req `extract.py` (FR-2) + park judgment-rungs behind the REQ-07 precision gate (FR-3) + complete REQ-01 FR-4 keyed lookup + `pattern_catalog sync` (FR-4) + register LOOP_CATALOG #8 (FR-5) | 📄 spec (`e3560ca3`) · **FR-5 LANDED** (LOOP_CATALOG #8 Review-Theme Metabolizer — the SDK-local slice) · **dev-os handoff written** for FR-1/2/3/4/6 (`HANDOFF_devos-req32-draft-time-firing-wire.md`, 2026-08-19; re-ground gate: dev-os on `chore/det-req-kit-learn-sdk-fields`, don't race it) | M, cross-repo (dev-os + CRP generator) | **REQ-25** parking machinery (`JudgmentRung`/`is_unparked`/`measure_precision`) verbatim; **prereq** for the rows below |
| **REQ-30 schema `Emits:` field** | serialization-artifact FR grammar (theme #3) + `serialization_gaps` fact-lint | 📄 **spec**, no code (handle `…-79047ab7`) | M, cross-repo | same class as the open **`Depends:` field (G-1)** — build as one det-req-kit grammar batch |
| **REQ-31 lifecycle `Lifecycle:` field** | state/lifecycle FR grammar (theme #6) + `lifecycle_gaps` fact-lint; det-plan process rollup grounded in shipped `checkpoint.py` | 📄 **spec**, no code (handle `…-addfb69f`) | M, cross-repo | `Depends:` field (G-1); det-plan `§2/§10` |
| **ambiguity / atomic-write / security fact-rungs** | the top-3 ship-ready theme lints (SYNTHESIS §3) — one predicate each, fire through REQ-32's seam | ⬜ backlog | S each | **security ≈ REQ-25 FR-1** (same `query_prime/security verify_file` reuse, draft-text altitude) |

> **Shared open questions with the REQ-22/23/25 tails** (don't re-solve): (a) the draft-lint **default-sweep-vs-opt-in**
> wiring is the *same* decision as "whole-liveness-layer govern wiring" (blocked-on corpus signal adoption); (b) the
> judgment-rungs need the *same* real LLM verify-live judge as the REQ-25 residual; (c) cousin of the unbuilt det-crp
> `crp_lint.py` (both fire CRP-derived rules on det-* docs, both grounded in CRP-INDEX).

> **REQ-14 numbering resolution (2026-08-16):** `REQ-14` = **control-region unification** (`REQ-14-control-region-unification.md`, BUILT `a84bf12c`). Two specs briefly collided under REQ-14 — a narrow `control-schema-formalization` (data-only, renderer-consumption deferred) and this broader *control + region* unification (steps 4+5, renderer consumption). The **unification was built** (it closes the wired-vs-inert mirror the integration review flagged), so it is the canonical REQ-14; the narrow `control-schema-formalization.md` is **superseded + removed**. The earlier "renumber the unification to REQ-15" note is retired.

---

## 🔧 Open follow-ups (mentioned, not started)

- **Reflective-family maturity ladder** (rungs 1–5) — audit family has one; reflective septet doesn't yet.
- **Remaining universal-but-SDK-homed principles** — README pointer table resolves citations, but
  promoting any (if deemed universal) is a clean one-at-a-time follow-on.
- **ContextCore Mottainai pointer** — could re-point to craft (trivial, not done).
- **Lacuna-audit findings** to act on: KM-refactor mid-flight, cross-layer backrefs, dormant sync
  tiers, vertebra symmetry (read-only, not fixed).

### Open harvest tails — hoisted from the ✅ rows (real open work that was buried in "done" cells)

> These are the still-open `(H1)/(H2)/…` tails embedded in the Implemented table. Hoisted so the open
> queue stops undercounting. **`scripts/verify_ledger.py` (2026-08-17) enumerates + liveness-checks the
> full set** — run it rather than trusting this hand-list. Effort in brackets.

**det-doc-kit arc:**
- ~~**det-crp `crp_lint.py`**~~ — **DONE** (`src/startd8/crp_lint/` + `scripts/crp_lint.py`, 12 tests; dogfooded clean over the 26-doc corpus). Fold-back: id-uniqueness is authoring-time not lint-time (references ≠ duplicates); header checks conditional-on-presence.
- **cross-repo det-req `Depends:` field (G-1)** — makes projected `dependsOn` real; the projector already consumes it, the gap is upstream in det-req-kit's grammar. `[M, cross-repo]` — **the det-req-kit grammar-field BATCH** (build together): `Depends:` (G-1, **validated by the 388-row/192-doc dependency-ordering theme** — biggest single demand) · `Emits:` (REQ-30) · `Lifecycle:` (REQ-31) · **`Config: precedence=`** (CLI theme) · **`Provenance:`** (159/114) · **`Budget:`** (134/89). All fact/judgment-split (REQ-25), all fire through REQ-32's seam. See `SYNTHESIS_crp-other-and-cli-mining.md`.
- **CRP census keyword fix (a $0 cheap win)** — add keywords to `render_crp_index.py`'s `REVIEW_THEME_RULES` (`golden`/`characterization`/`parity`/`drift`/`round-trip`→determinism · `explicit`/`implicit`→ambiguity · `protocol`/`interface`/`signature`→schema · `threat model`/`trust boundary`/`egress`/`allowlist`→security) → reclassifies ~530 "other" rows into their real themes, sharpening every future count. `[XS, cross-repo dev-os, CRP-INDEX owner]`
- **det-howto pairsWith path-value convergence** — the *line format* is shared now, but howto stores a full path while plan/handoff store a basename (both resolve; a minor projector-choice divergence). `[XS]`
- **provider skip-path unexercised** — the 3 projectors' `is_in_sync` `$0`-skip is unit-tested but never driven by a real prime-contractor run (an integration test would prove the `$0` claim end-to-end). `[S]`
- **push the det-doc-kit arc to `origin`** — it's on local `main` only (through `72fdd740`). `[XS, human call]`

**cross-surface (2026-08-19):**
- **REQ-cross-surface H1 — cockpit adopter** — **AUTHORED** (`REQ-cockpit-surface-links-adopter.md` + `PLAN-cockpit-surface-links-adopter.md`). Wires cockpit readiness tiles to consume `surface_links.drill` and render `resolve_surface_link_href`-based links to the navig8r `#<key>` route. Web + Grafana v2 in scope; terminal deferred (no per-field rows). `[XS–S implement]`

**numbered REQ-01..25 arc (still-open per the rows' own notes):**
- **Whole-liveness-layer `govern` wiring** (REQ-22/23/25 shared) — `check_liveness_layer`'s cells aren't in `govern_corpus`'s default sweep. **`Gate:` unblocked by REQ-27** (9/9 mechanical gated); still blocked on corpus **`Signal:`** adoption (target-unmeasured / served-by-dead cells). `[M, blocked-on: corpus Signal: adoption]`
- **REQ-19 H2** — `corpus_realization` `grounded` is all-or-nothing → one measured node labels the whole % `measured`; could be `measured (partial)`. `[S]`
- **REQ-25 residual** — plug a real (LLM) verify-live judge into the parked judgment-rungs (the gate around it is done + fixture-proven with a mock). `[M, optional]`
- **cross-repo mirrors** — `dev-os/NODE-SCHEMA.md` §1 + CC Node mirror to 0.4.0 (REQ-16/17); the `verify.gate` det-req-kit field (REQ-22 NR-5). `[M, cross-repo, coordinated handoff]`
- **REQ-18 H1/H2/H3** — cosmetic mixed-count in the %-string; latent `O(n²)` `_facets_html` on deep trees; a silent `except Exception: pass` in govern's invariant-9. `[XS each]`

---

## ⏸️ Explicitly deferred (NOT open — for completeness)

- REQ-01 lens-lift is captured as **REQ-04** (so REQ-04 is the live path, not deferred).
- Uncommitted craft session docs (`THE_CRAFT_GRAMMAR`, `THE_NATURAL_LANGUAGE_PROGRAMMING_SYSTEM`,
  `LACUNA_AUDIT_findings`) + modified `KAGAMI` — **left for the user** to place/version, deliberately
  not committed.

---

## Recommended next pickups (priority order)

### Open queue — the live pick list for the Spec Delivery Loop (reseeded 2026-08-19)

> **Why this table exists.** The inflection-arc table below is fully SHIPPED, so the ledger had **no queue
> left to pick from** while four stage-0-gated specs sat on disk unlisted — `verify_ledger.py` audits only
> the ✅ rows (survivor census), so drift accumulated on the **open** side where nothing was watching. Every
> row below was re-grounded 2026-08-19: stage-0 gate re-run (`navigator_spec_delivery_loop.py --status`) and
> the implementation confirmed **absent** from `src/startd8/` (not merely unrecorded, as REQ-26 was).
> Same column contract as below — fill `Claim` with `agent · since <date>` when you start, clear it if you drop it.

| # | Spec | Track | State | Effort | Claim | Handoff / note |
|---|------|-------|-------|--------|-------|----------------|
| 1 | `REQ-cross-surface-view-definition` | cross-surface | ✅ **SHIPPED** (`67051859`) | M | done | 19th loop delivery. Shared `node_state` + `surface_links` on the existing cascade; navig8r statuses project from the shared taxonomy (Derive-to-Prove); cockpit declared-not-wired (NR-7). 73 FR+golden tests; byte-identity UNEDITED. **Follow-on:** cockpit adopter (H1) |
| 2 | `REQ-27-self-dogfood-verify-gate-adoption` | liveness | ✅ **SHIPPED** (`7e26e3ff`) | M | done | 20th loop delivery. Corpus Gate: adoption 9/9 mechanical (rate 1.0); Manual: on REQ-22; advisory `--self-dogfood`. Partial unblock of whole-liveness govern wiring (Gate: yes; Signal: still open) |
| 3 | `REQ-28-runtime-o11y-grounding-into-the-loop` | grounding | 📄 spec, **stage-0 ✓** (6 FRs) | M–L | — | no handoff yet. Routes runtime observability into the loop (the territory edge) — fills REQ-19's realization seam from the *running* side and extends the REQ-22/23 liveness layer; retrospective destination is REQ-20 |
| 4 | `REQ-green-evidence-ir` | IR | 📄 spec, **stage-0 ✓** (10 FRs) | L | — | no handoff yet. SARIF's missing complement — a first-class IR for what is PROVEN, with liveness provenance. Largest open spec; generalizes REQ-22 per-node liveness to corpus scale and gives the determinism-% a home. Consider `/reflective-requirements` before the loop (REQ-24 precedent) |
| 5 | REQ-32 FR-1/2/3/4/6 | det-doc-kit | 📄 spec · **FR-5 LANDED** (`44bdb18b`) | M | **dev-os** | cross-repo, NOT an SDK pick — handoff written (`HANDOFF_devos-req32-draft-time-firing-wire.md`). **Re-ground gate: dev-os is on `chore/det-req-kit-learn-sdk-fields` — don't race it** |
| 6 | REQ-30 `Emits:` · REQ-31 `Lifecycle:` | det-doc-kit | 📄 spec, stage-0 ✓ | M each | — | **hard-blocked on #5** (both fire through REQ-32's draft-time seam). Build as one det-req-kit grammar batch with the `Depends:` tail — see §Open harvest tails |
| — | harvest tails | mixed | see §Open follow-ups | XS–M | — | the `(H1)/(H2)` tails hoisted out of the ✅ rows; `scripts/verify_ledger.py` enumerates them. Pick one when the queue above is claimed |

> **Order:** #1 SHIPPED 2026-08-19 (`67051859`). #2 SHIPPED 2026-08-19 (`7e26e3ff`). Next: #3 (REQ-28) or
> #4 (REQ-green-evidence-ir — largest/reflective). #5/#6 are cross-repo and gated — leave them to dev-os.

### Loop backlog — the 2026-08-17 inflection arc (COMPLETE — kept for provenance)

> The 2026-08-17 **two-sided-coin inflection** (`STRATEGY_navig8r-inflection-two-sided-validation.md`)
> opened a NEW arc on top of the REQ-01..27 one. Each spec below passes the Spec Delivery Loop stage-0
> gate and has a self-contained `HANDOFF_*.md`. **Two independent tracks** — card-browse filters, and
> graph/composition. Take a spec through the loop from its handoff; land via the hot-main ff cadence.

> **Columns (2026-08-17):** added **Effort** (XS/S/M/L) and **Claim** (which session/agent holds it — this repo
> runs many concurrent worktrees, so an unclaimed item is fair game; a claimed one is in-flight — don't double-pick).
> Fill `Claim` with `agent · since <date>` when you start; clear it if you drop the item.

| # | Spec | Track | State | Effort | Claim | Handoff / note |
|---|------|-------|-------|--------|-------|----------------|
| 1 | `REQ-unify-card-visibility-predicates` (Move 3) | card-browse | ✅ **SHIPPED** (`40efeac6`) | S | done | one `applyVisibility()` recompute + non-clobbering hide-reasons + survivor-set `pagedCards()` (fixed the `pagedCards`↔`pf-hidden` bug) + the FR-5 seam; 7 FR pins green |
| 2 | `REQ-freetext-search-on-navigator-card-browse` | card-browse | ✅ **SHIPPED** (`49db7e91`) | M | done | rode Move 3's seam — collapsed to registering `srch-hidden` as one predicate (FR-4/5 by construction); definition-owned `search` group; 7 FR pins green |
| 3 | `REQ-audience-tiered-disclosure-on-navigator-surfaces` (Move 2) | card-browse | ✅ **SHIPPED** (`9545c951`) | M | done | specced reflectively then built — planning falsified "audience = a predicate"; it's a RE-RENDER disclosure via the existing lens. `discloseTier()` + a one-place tier→fields seam; the doc-context band tiers minimal→maximal (the deferred pare = a tier, not a delete). 6 FR pins green. **Card-browse track COMPLETE (Move 3 → search → Move 2).** |
| A | `REQ-feature-capability-composition-rollup` | graph/grammar | ✅ **SHIPPED** (`726405b3`) | S–M | done | mostly REUSE as predicted — the generic `serves` edge already drew it; the gate was 3 seams: `_SERVES` widened to CAP-*/dotted + new `_COMPOSES` field (O-N back-compat held), `--source requirements+capabilities` join, `--rank-direction ground-up` (`_ground_up_rank_y`; default layout byte-identical). 9 FR pins green. The SDK dogfood of ContextCore EB-4. **CEP note:** the provider `is_in_sync` skip-path (from det-plan) still not proven by a real prime run — carried forward |
| B | Move 1 — navig8r as the hub (cross-topology links) | graph | ✅ **SHIPPED** (`00e7181a`) | L→S | done | right-sized by the loop from "auto-link to all topologies" (infeasible — only a11y has anchors, no filename convention → dead links) to an **authored opt-in** `--cross-link src=href` seam + a cross-topology row in `buildFullView`. **Graph track COMPLETE (composition primitive → Move 1).** |

> **Order:** card-browse = Move 3 → search → Move 2; graph = composition primitive → Move 1. The two
> tracks are independent. **BOTH TRACKS COMPLETE (2026-08-18):** card-browse (Move 3 `40efeac6` → search
> `49db7e91` → Move 2 `9545c951`) and graph (composition `726405b3` → Move 1 `00e7181a`). **Already shipped
> this arc:** node-fields distillation (`ce6ed667`) + requirement detail view (`788042a8`).
> *Cross-repo sibling (ContextCore, not the loop):* `REQ-contextcore-objective-serves-edge.md` — the same
> composition primitive, objective→objective (the SDK side now proves it; ContextCore EB-4 is the twin).

---

> **Reconciled 2026-08-17 (twice — the first pass was itself stale).** A full build-status review found
> the arc far more complete than the ledger claimed. **Already built (do NOT pick up):** REQ-03 a11y
> (`d416fc38`) · REQ-04 lift-lenses (`5cb91c37`) · **REQ-15 frame view** (`ea65febf`) · **REQ-19
> live-wiring / measured-provenance fuel** (`b2dea22d` — `generate backend --emit-realization-provenance`
> writes the artifact; `build/govern --realization-provenance` load it). The recurring lesson: verify by
> FR-tag commits + tests, never by this list.

**Deferred-by-design tails of the REQ-01..27 arc** (the *new* inflection backlog is the table above; the original numbered arc is complete bar these tails):

1. **REQ-25 judgment-rungs — un-park MECHANISM now grounded** (`785fb317`): the **labeled fixture set** +
   precision-measurement harness shipped, so a rung's `precision` is now MEASURED over 24 labeled cases
   (not a hand-passed float); an accurate verify-live judge un-parks and emits candidates, a wolf-crier/
   below-threshold/non-verify-live judge stays parked (proven with a mock judge, no LLM). The ONLY residual
   is **plugging a real (LLM) verify-live judge** — genuinely optional, needs the LLM + FR-6 wiring; the
   whole gate around it is done and proven. (Fixture DATA can also be extended — it's just more rows.)
2. **Whole-liveness-layer `govern` wiring** (shared REQ-22/23/25 follow-on) — `check_liveness_layer`'s
   verify-liveness / target-unmeasured / served-by-a-dead-FR cells aren't in `govern_corpus`'s default
   sweep (wiring them = **60 findings** on the current corpus, which isn't authored with `Signal:`/`Gate:`
   yet). The REQ-25 fact-rungs ARE wired (opt-in, silent today). Needs the corpus to adopt signals first.

> **REQ-25 fact-rungs CLOSED 2026-08-17** (`2097d43e`): the three hypothesis cells' **fact-rungs** ship as
> GAP/trigger reusing existing checkers (mitigation-inert → `query_prime/security verify_file`;
> non-goal-violated → the `ast` import walk; touches-provenance-changed → REQ-19 provenance-change), plus
> the judgment-rung **parking machinery** (`JudgmentRung`/`is_unparked`/`run_judgment_rung` — parked by
> default; un-park needs precision ≥ threshold AND a verify-live judge; un-parked emits a CANDIDATE, never
> a GAP). Registered in `check_liveness_layer` (Tier-2 behind Tier-1); GAPs route to a proposed human-gated
> Lesson. Wired into `govern_corpus` opt-in → byte-identical on the real corpus (0 findings, exit 0). 427
> navigator tests (11 new). Build-time discovery: the reused ast walk missed `from X import Y`'s name — fixed.

> **REQ-24 H2 CLOSED 2026-08-17** (`144761ca`): the byte-identity revise guard is now pluggable across
> the deterministic `$0` family (`--kind backend|scaffold`, extensible to views/frontend); a
> non-deterministic / polyglot / LLM kind has no regenerator → the revise fails safe to `human` (byte-
> identity is unprovable for LLM output — REQ-21's own principle, not a gap). Build-time discovery: the
> provenance-marker set only stripped 3 of 11 `$0` source-fingerprint stamps, so scaffold could never
> pass — fixed to strip the whole family.

> **Requirement-detail-on-card + node-fields distillation SHIPPED 2026-08-17** (`ce6ed667` distillation +
> `788042a8` rendering): the navigator requirement card gains an inline detail **peek** and a new full-page
> requirement **view** — surfacing far more of a requirement's authored data. First a behaviour-preserving
> **distillation**: the card built its data by flattening structured `Node` fields into a `detail` prose
> blob (11 prefix markers) then regex-reparsing it client-side — an untyped contract mirrored across Python
> (`_node_detail`) and JS (`structuredDet`+`_SLOT`). Replaced by one structured extraction (`_node_fields`)
> the card reads BY KEY (`fieldsToSd`); `detail` is now FORMATTED FROM it, so the text/JSON contract can't
> drift. Then **richer rendering** on that seam: a doc-context masthead band (criticality/domain/audience/
> risks with mitigation→FR coverage — kept MAXIMAL; pare is an audience/fluency lens tier, not a delete), a
> full **typed Touches** list (`WireframeItem.touches` + a source-bound path→kind classifier), the inline
> **peek** (click-to-expand record + Touches; `open full view →` at the status pill + top + bottom), and a
> full-page **client-side `#<key>` route** (`buildFullView`/`resolveHash`; deep-link on load; back-to-
> browse). One shared `recordEntries`/`touchesRows` feeds both views (no drift). New regions `docband`/
> `detail`/`fullview` registered in the View Definition `regions.bindings` (FR-8). Spec
> `REQ-requirement-detail-on-navigator-card.md` v0.3; 9 guard tests; 637 navigator+wireframe green; app-path
> byte-identity held unedited. Build-time discovery: the card click bailed on the `<details class="sec">`
> **ancestor** wrapper (`closest("details")`), swallowing every click — fixed to `w.contains(hit)` (inside-
> card only) with a regression pin. Landed via a double-rebase onto a hot `main` + `--ff-only`.

> **Free-text card search SPEC BUILD-READY + variant inventory 2026-08-17**: an audit of the six renderers (tree/graph/a11y/index/diff/wireframe) as distinct SOURCE×TOPOLOGY×PRESENTATION×AUDIENCE cells (`VARIANT_STATUS_navigator-renderer-inventory.md`) found free-text search the top pull-in gap (tree has it, the card view had 0). Authored `REQ-freetext-search-on-navigator-card-browse.md` (reflective det-req, 7 FRs, passes the Spec Delivery Loop stage-0 gate) + a loop handoff for a separate build session. Grounding surfaced a pre-existing bug — `pagedCards()` ignores `pf-hidden`, so status filter and paging don't intersect on the paged count today — folded into FR-5. Deprecation list stays narrow: only the ur-form hand-markdown navigators + the removed REQ-14-schema spec.

> **navig8r strategy + Move 3 spec BUILD-READY 2026-08-17**: framed navig8r as a **two-sided validation surface** — technical grounding (verification-that-can't-die) AND human/business value (readable/navigable/judgeable by the right audience) — in `STRATEGY_navig8r-inflection-two-sided-validation.md`, with the **Move 3 → 2 → 1** direction of record (distill visibility predicates → audience-first tiering → hub cross-links). Authored `REQ-unify-card-visibility-predicates.md` (Move 3, 7 FRs, BUILD-READY) + handoff: one `applyVisibility()` recompute, non-clobbering per-predicate hide-classes, survivor-set paging (SUBSUMES the search REQ's FR-5 `pagedCards` fix), a plug-in seam for search + audience. Moves 2/1 are outlined, to be reflectively specced against Move 3's landed seam. Loop order: Move 3 → search → Move 2 → Move 1.

> **Feature→capability composition + ground-up rollup SPEC BUILD-READY 2026-08-17**: authored `REQ-feature-capability-composition-rollup.md` (6 FRs, passes the loop gate) + handoff — a **reusable composition primitive**: an FR declares the capability it builds up to, rendered ground-up (features at the base). Mostly REUSE — the `serves` edge is already drawn generic-on-target (`graph_projection.py:181`), the FR serves-attr already flows (`sources_requirements.py:303`); the one gate is the det-req parser `_SERVES` (`det_req.py:31`, O-N only) → extend to capability targets, join FR+capability sources, add a bottom-up rank. The SAME primitive is ContextCore's EB-4 (objective→objective) — one edge, both corpora. Orthogonal to Move 3/search; sits with Move 1. Grounded in both repos: the SDK's FRs don't roll up to capabilities either (FRs Serve only local O-N).
