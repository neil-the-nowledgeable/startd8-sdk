# View Definition View Mode & Control-Surface Consolidation — Requirements

**Project:** startd8-sdk   **Criticality:** medium
**Version:** 0.2 (post-planning — self-reflective update)   **Date:** 2026-08-16
**Format:** det-req/0.1
**Backend:** python-cli-surface
**Pairs with:** *(plan deferred — spec-only; delivered via the Spec Delivery Loop)* · architecture `ARCHITECTURE_navig8r-presentation-definition-inheritance.md` (§3 regions/layers · §7 steps 4/5)
**Inherits standards:** det-req-kit · NODE-SCHEMA v0.3.9 · NAMING_CONVENTION · REQ-10-view-definition-cascade (keystone) · REQ-14-control-region-unification (parent — this reshapes its `control` groups) · REQ-15-frame-scaffolding-view (parent — this promotes its frame from a CLI source to an in-render mode) · SOTTO_DESIGN_PRINCIPLE
**Audience:** operator / SDK contributors / cross-repo adopters (legal · benchmark · dev-os)
**Trust boundary:** local render only; no network; no LLM
**Data classification:** internal

> **Readable handle:** `feature/sdk-navigator-view-definition-mode-and-control-consolidation`
> **Semantic name:** *SDK navigator exposes the View Definition as an in-render pick in the primary View picker — showing the current renderer's own requirement-free frame in place opposite the Requirement render — and collapses the top-right control surface to one View picker plus one additive Overlays stack, retiring the Structure/Combined density modes (their per-node metadata preserved as a Show-node-metadata overlay) and the scaffold-only duplicate, with the taxonomy and labels owned by the View Definition.*
> **Canonical ref:** `cc:intent:requirements-visualization:feature:view-definition-mode-and-control-consolidation`

## 0. Why this exists — see the View Definition of the thing you're looking at

REQ-14 lifted the control panel + region anatomy into the View Definition; REQ-15 added a domain-neutral
**frame** (`--source frame`) that renders the bare scaffolding free of any requirement. Two gaps remain:

1. **The frame is only reachable as a separate CLI invocation with zero nodes.** There is no way, while
   looking at an actual render (say REQ-10 visualized), to say *"show me the View Definition this view is
   built from"* — the requirement-free frame **of that renderer**, in place. The operator wants to flip
   between the requirement and its underlying View Definition.
2. **The top-right control panel has accreted overlapping controls.** `Structure only` (View picker),
   `Scaffold mode` (Template anatomy), and `Scaffold only` were three separate early gropings toward the
   same idea REQ-15's frame now expresses maturely — *"show the structure/definition, not the requirement
   content."* The `View · pick one (Full default)` group and the `Template anatomy · debug` group overlap,
   and the labels are stale. They need consolidating onto clean axes — **without losing any distinct
   control** (the operator wants maximum controls, just no duplicates).

This REQ (a) promotes the frame into a first-class **"View Definition" view mode** driven from the control
surface, using the **current renderer's own resolved definition**; and (b) consolidates the control panel
onto four non-overlapping axes, retiring the true duplicates and reframing the rest, with the taxonomy
owned by the definition (extending REQ-14) so the consolidation is data, not a hardcoded panel.

## 0. Planning Insights (self-reflective update)

> Grounding the ask against the real control surface (`view_definition.py` `control`, `_template.py`
> `#debug` panel, the `body.structure-only`/`body.scaffold`/`body.frame-bare` classes) revealed:

| v0.1 assumption | Planning discovery | Impact |
|-----------------|--------------------|--------|
| "Expose the View Definition view" = surface the existing `--source frame` | `--source frame` renders **`BASE_NAVIG8R_DEFINITION` only** (`cli_navigator.py` frame branch). "The View Definition **of a renderer**" means the frame of the **current domain's resolved definition** (requirements/capability/…), not base. | **FR-1**: the in-render mode renders the frame of the *current* resolved definition; `--source frame` (base) stays as the domain-neutral CLI entry (**FR-4** — one shared mechanism, two entry points) |
| The overlapping controls are just mislabeled | The panel tangles **two distinct axes** into one picker: **(1) what am I looking at** — the Requirement vs the requirement-free View Definition (high-value, binary); **(2) how much of the requirement** — Full/`structOnly`/`combined` density. `structOnly`/`scaffold`/`scaffoldOnly` were all early attempts to answer axis 1 with axis-2 tools. The only *unique content* the two density modes add is `item.meta` (a per-node structural-metadata string; `_template.py:623`) — both reveal it, they differ only in prose. | **FR-2**: separate the axes and **collapse axis 2** — the picker becomes the binary **Requirement / View Definition**; `combined` is dropped (Full + meta), `structOnly`'s only unique payload (`item.meta`) survives as an **additive "Show node metadata" overlay**, and `scaffoldOnly` folds into "View Definition". Net panel = **1 pick-one + 1 overlay stack** (was 3 overlapping groups) |
| The consolidation preserves byte-identity like REQ-14/15 | REQ-14/15 were **additive-override** (panel unchanged → literal-string test unedited). This REQ **intentionally changes the panel** (relabels groups, removes `scaffoldOnly`, adds "Show View Definition"). The debug-panel literal-string test **cannot** stay unedited — it is the one deliberate, reviewed change. | **FR-5**: the load-bearing byte-identity (app-scaffold path + domain **node content**) still holds — the debug panel is profiled/navigator-only, never in a generated app — but the **panel test is updated on purpose** (the change under review), not preserved |

**Resolved open questions:** OQ-1 (does "Show View Definition" use base or the current definition?) →
the **current** resolved definition (FR-1); base stays on `--source frame`. OQ-2 (does consolidation drop
controls?) → yes, deliberately — the `combined` and `structOnly` *density modes* are retired (their
only unique payload, `item.meta`, becomes a "Show node metadata" overlay); `scaffoldOnly` folds into
"View Definition"; the one intentional loss is prose-stripping-for-compactness (NR-3). OQ-3 (can the app
path change?) → no — the debug panel is navigator-only; the app path + node content stay byte-identical (FR-5).

## Overview

Add a **"View Definition"** entry to the top-right control panel's primary picker that renders the
**current renderer's** resolved View Definition as a requirement-free frame **in place** (scaffold-on,
node content hidden, regions + their definition-owned meta-descriptions + the control surface — no
requirement nodes), reusing REQ-15's frame mechanism. The existing `--source frame` (base definition) and
this in-render mode share **one** render path (FR-4). Reshape `BASE_NAVIG8R_DEFINITION.control.groups` from
the current three overlapping groups (`view` / `overlays` / `template-anatomy`) into **one pick-one picker
+ one additive overlay stack**: **View** (pick one: **Requirement** [default] · **View Definition**) and
**Overlays** (additive: **Show node metadata** · **Outline regions** · the REQ-15 **per-layer disclosure**
· **Hide app-scaffold chrome**). This retires the `combined` and `structOnly` *density modes* — folding
their only unique payload (`item.meta`) into the **Show node metadata** overlay — and folds `scaffoldOnly`
into the View Definition view. All taxonomy/labels are owned by the definition and rendered via the REQ-14
additive-override seam. Additive to generated apps; the navigator's own debug panel is the one surface
that (deliberately) changes.

## Objectives

- **O-1:** Expose the View Definition of a renderer in-render — target: a control toggle renders the current domain's requirement-free frame in place, and flips back to the requirement.
- **O-2:** Consolidate the control surface — target: one pick-one **View** picker (Requirement / View Definition) + one additive **Overlays** stack; the `combined`/`structOnly` density modes retired (their `item.meta` payload preserved as a "Show node metadata" overlay) and `scaffoldOnly` folded; stale labels fixed.
- **O-3:** Definition-owned taxonomy — target: the consolidated groups/toggles/labels are `control` data in the View Definition (extending REQ-14); a domain delta can relabel them; nothing panel-facing is hardcoded.
- **O-4:** One frame mechanism — target: the in-render "Show View Definition" mode and `--source frame` share a single render path (current-definition vs base are the only difference).
- **O-5:** No app regression — target: the app-scaffold path and domain **node content** are byte-identical; only the navigator debug panel changes (its literal-string test updated under review).
- **O-6:** The View Definition shows ALL display-logic — target: when the View Definition is shown, **every** region renders the display logic for what it displays (its field-slot template + what it derives from) — not just the node region — so no region is blank and the frame fully meta-describes both WHERE each region sits and HOW/FROM WHAT it renders.
- **O-7:** Richer, honest debug mode — target: the frame's provenance readout reads as a definition summary (not false "cruft") when no requirement is loaded; and a **Debug** control group exposes the raw data + nodes being visualized in a panel below the export sign-off.
- **O-8:** Functional display capabilities are enabled through the View Definition — target: the View Definition owns not just presentation (labels/layout/theme) but **functional** display capabilities, starting with **paging** — a definition-owned control to show N requirement nodes at a time and page through the rest (down to one at a time) — so a domain governs how much content shows and how the reader navigates it.

## Risks

| Type | Description | Mitigation | Priority |
|------|-------------|------------|----------|
| quality | Reshaping the panel silently changes the app-scaffold render | FR-5: the debug panel is profiled/navigator-only — the app path has none; assert app-path + node-content byte-identity unedited | high |
| scope | Over-collapsing merges away a useful control | NR-3 + FR-2: "max controls, no duplicates" — keep every distinct effect (Full/Structure/Combined/Outline regions/Hide chrome/per-layer); fold **only** the `scaffoldOnly` true duplicate | high |
| quality | The in-render frame leaks the domain's requirement nodes (defeats "requirement-free") | FR-1 + NR-4: the mode reuses REQ-15's content-hidden frame; it renders the definition's scaffolding, never the domain nodes | medium |
| scope | Building a control DSL / storing toggle JS in the definition | NR-1: the definition declares *what* controls exist; the class-toggling interaction JS stays renderer-specific (REQ-14/15 NR-1 carried) | high |
| quality | Two frame render paths drift | FR-4: one shared mechanism; in-render passes the current resolved definition, `--source frame` passes base | medium |

## Functional requirements

- **FR-1 — "View Definition" as an in-render pick (of the current renderer).** The top-right panel's primary **View** picker gains a **View Definition** option (pick-one against **Requirement**, the default) that re-renders the current view as its **own** resolved definition's requirement-free frame in place — scaffold-on, node content hidden, showing each region's definition-owned meta-description (`regions.bindings[id].scaffold`) + the control surface — and selecting **Requirement** restores the requirement render. Name: The navigator View picker offers a View Definition option that renders the current renderer's requirement-free frame in place and Requirement restores it. Touches: `src/startd8/wireframe_view/_template.py`, `src/startd8/navigator/view_definition.py`, `tests/unit/wireframe/test_render_profile.py`. Lives: code src/startd8/wireframe_view/_template.py. Approve?: can the operator flip a live requirement render to its underlying View Definition frame and back?. Verify: a profiled requirements-domain render exposes a `data-view="view-definition"` pick; selecting it adds the frame body classes (`scaffold` + content-hidden) and hides FR node content while the region meta-descriptions remain; selecting Requirement restores the requirement render. Serves: O-1
- **FR-2 — Collapse the control panel to one View picker + one Overlays stack.** `BASE_NAVIG8R_DEFINITION.control.groups` is reshaped from `view`/`overlays`/`template-anatomy` into exactly two groups: **`view`** (pick-one: **Requirement** [default] · **View Definition**) and **`overlays`** (additive: **Show node metadata** — reveals the per-node `item.meta`, the sole unique payload of the retired density modes · **Outline regions** — the region-outline annotation, was `scaffold` · the REQ-15 **per-layer disclosure** · **Hide app-scaffold chrome**); the `structOnly` and `combined` *density modes* and the `scaffoldOnly` toggle no longer exist as controls. Name: The control panel collapses to one pick-one View group Requirement or View Definition plus one additive Overlays group with density modes retired and item.meta preserved as a Show node metadata overlay. Touches: `src/startd8/navigator/view_definition.py`, `tests/unit/navigator/test_view_definition.py`. Lives: code src/startd8/navigator/view_definition.py. Approve?: is the panel one View picker plus one Overlays stack, with density modes retired and item.meta preserved as an overlay?. Verify: `resolve(BASE_NAVIG8R_DEFINITION, DEFINITION_REGISTRY).control["groups"]` keys include `{"view", "overlays"}` (plus the FR-8 `debug` + FR-9 `paging` groups); the `view` group's toggles are `{"requirement", "view-definition"}`; the `overlays` group holds a `nodeMeta` toggle and no `structOnly`/`combined`/`scaffoldOnly` toggle exists in any group; enabling `nodeMeta` reveals `item.meta` on the current render. Serves: O-2
- **FR-3 — The consolidated taxonomy + labels are definition-owned.** The reshaped groups/toggles/labels/hints live in the definition's `control` section and render via the REQ-14 additive-override seam, not hardcoded panel HTML; a domain delta can relabel a group or toggle. Name: The consolidated control taxonomy and labels are owned by the View Definition and render via the additive-override seam. Touches: `src/startd8/navigator/view_definition.py`, `src/startd8/wireframe_view/_template.py`, `tests/unit/wireframe/test_render_profile.py`. Lives: code src/startd8/wireframe_view/_template.py. Approve?: is the panel taxonomy/labels definition data a delta can override, not hardcoded?. Verify: the rendered panel's group/toggle labels equal `profile.control` values; a definition delta renaming the `content` group's label to "Density" renders "Density" in the panel; removing the delta renders the base label. Serves: O-3
- **FR-4 — One frame mechanism, two entry points.** The in-render "Show View Definition" mode and `navigator build --source frame` share a single frame-render path; the only difference is the definition supplied — the **current** resolved definition for the in-render mode, `BASE_NAVIG8R_DEFINITION` for the CLI source. Name: The in-render View Definition mode and the frame CLI source share one render path differing only by which definition is supplied. Touches: `src/startd8/navigator/cli_navigator.py`, `src/startd8/wireframe_view/_template.py`, `tests/unit/navigator/test_sources_and_cli.py`. Lives: code src/startd8/navigator/cli_navigator.py. Approve?: is there one frame mechanism, not two?. Verify: rendering `--source frame` and selecting the View Definition pick on a base-definition render produce the same frame body classes + region-meta structure; no second frame renderer/template exists (grep: one content-hidden frame path). Serves: O-4
- **FR-5 — Additive to apps; panel change is the one reviewed edit.** The app-scaffold path and domain **node content** stay byte-identical (the debug panel is profiled/navigator-only, never emitted into a generated app); the byte-identity self-comparison + node-field goldens pass unedited, while the debug-panel literal-string test is **updated** to the consolidated taxonomy as the deliberate change under review. Name: The consolidation is additive to generated apps and node content with only the navigator debug-panel test updated on purpose. Touches: `tests/unit/wireframe/test_render_profile.py`, `tests/unit/navigator/test_view_definition.py`. Lives: test tests/unit/wireframe/test_render_profile.py. Approve?: are apps + node content byte-identical, with only the panel test intentionally changed?. Verify: `test_no_profile_is_byte_identical` + `node_field_names()` golden pass UNEDITED; a requirements-domain render's **node content** is byte-identical to its pre-REQ golden; the debug-panel test is updated (not deleted) and asserts the new four-axis taxonomy. Serves: O-5
- **FR-6 — The View Definition shows EVERY region's display-logic template.** When the View Definition is shown (the frame), **every** region — not just the node one — renders a slot-annotated display-logic template built from the real render classes, showing what it displays and FROM WHAT it derives: masthead (`‹eyebrow›`/`‹headline›` ← profile chrome; `‹why›`/`‹do›` reading guidance), glance band (`‹status roll-up›` ← `status_counts` · `‹shape›` ← `plan.shape` · content · cascade — computed), control layer (`‹View›` ← audience/role · `‹Depth›` ← fluency lens), status legend (`‹dot›`+`‹meaning›` ← `profile.statuses[]`), section lead (`‹section_lead›` ← `profile.section_lead`), and the node-driven outline (the node-card field template — `.lbl`←label · `.lbl-key`←key · badge←status · `.det`←detail · `.lives`←`type:ref` · `.node-meta`←meta); shown by DEFAULT when the frame is active (not blank), injected client-side in frame mode ONLY so a normal render's served HTML + the app path stay byte-identical. Name: The View Definition renders a display-logic template for every region annotating its field slots and derivation source as default frame behavior. Touches: `src/startd8/wireframe_view/_template.py`, `tests/unit/wireframe/test_render_profile.py`. Lives: code src/startd8/wireframe_view/_template.py. Approve?: does the frame show HOW and FROM WHAT every region renders (no blank regions), not just the node region?. Verify: a `--source frame` render injects a `.vd-template` into each region (`#mast`/`#glance`/`#toolbar`/`#legend`/`#seclead`/`#outline`) carrying that region's slot annotations + source; the outline template uses the node-card classes; a normal requirements-domain render still shows real content with no template injected; `test_no_profile_is_byte_identical` passes unedited. Serves: O-6
- **FR-7 — Frame provenance readout is definition-aware (no false cruft).** When the render is the bare frame (`payload.frame`), the debug panel's provenance readout does NOT flag the empty chrome slots as "cruft" (there is no real content to be cruft in a requirement-free View Definition) — it reads as a definition summary instead (e.g. the region/layer count); a normal requirement render keeps the real chrome-provenance/cruft readout unchanged. Name: The frame provenance readout reads as a definition summary rather than false cruft when no requirement is loaded. Touches: `src/startd8/wireframe_view/_template.py`, `tests/unit/wireframe/test_render_profile.py`. Lives: code src/startd8/wireframe_view/_template.py. Approve?: does the View Definition frame stop reporting its own empty chrome slots as cruft?. Verify: a `--source frame` render's `.dbg-prov` readout contains no "cruft" text and instead a definition summary; a profiled requirements render still shows the chrome-provenance score + any real cruft; the app path is byte-identical. Serves: O-7
- **FR-8 — Debug group: raw data + node data views below the sign-off.** The control panel gains a definition-owned **Debug** group with a **Raw data** toggle (reveals the full payload/variant being visualized) and a **Node data** toggle (reveals just the node items being visualized), each rendering a raw-JSON block into a `#rawdata` panel placed **below the export sign-off**; both default off and are additive (no view change), and the panel/JSON is profiled-navigator-only so the app path stays byte-identical. Name: A Debug control group exposes Raw data and Node data toggles that render the raw payload and node items into a panel below the export sign-off. Touches: `src/startd8/navigator/view_definition.py`, `src/startd8/wireframe_view/_template.py`, `tests/unit/wireframe/test_render_profile.py`, `tests/unit/navigator/test_view_definition.py`. Lives: code src/startd8/wireframe_view/_template.py. Approve?: does a Debug group expose the raw data + nodes being visualized in a panel below the sign-off, additive + app-path-safe?. Verify: `resolve(BASE_NAVIG8R_DEFINITION, DEFINITION_REGISTRY).control["groups"]` includes a `debug` group whose toggles include `{rawData, nodeData}` (+ the FR-10 `inspectCells`); a profiled render has a `#rawdata` panel positioned after `#signbar`, hidden by default; enabling Raw data fills it with the current variant's JSON and Node data with the node items' JSON; `test_no_profile_is_byte_identical` passes unedited. Serves: O-7
- **FR-9 — Paging over the requirement nodes (definition-owned).** The control panel gains a definition-owned **Paging** group with a pick-one page size — **All** (default) · **10** · **5** · **1** — that shows only that many requirement node cards at a time; when a finite size is active a **paging bar** (`‹prev›` · "showing X–Y of Z" · `‹next›`) renders below the node outline to page through the rest, **down to one requirement at a time**, and any section with no visible card on the current page hides its header; the page-size options are definition-owned (a domain can configure them), and the capability is additive — default All, profiled-navigator-only, so the app path stays byte-identical. Name: A definition-owned Paging control shows N requirement nodes at a time with a prev/next bar to page through the rest down to one at a time. Touches: `src/startd8/navigator/view_definition.py`, `src/startd8/wireframe_view/_template.py`, `tests/unit/wireframe/test_render_profile.py`, `tests/unit/navigator/test_view_definition.py`. Lives: code src/startd8/wireframe_view/_template.py. Approve?: is paging a definition-owned control that shows N reqs at a time and pages through the rest down to one, additive + app-path-safe?. Verify: `resolve(BASE_NAVIG8R_DEFINITION, DEFINITION_REGISTRY).control["groups"]` includes a `paging` group whose pick-one toggles include a one-at-a-time option; a profiled render with a finite page size shows only that many `.item` cards + a `#pagebar` with prev/next and a total count, and `next` advances the visible slice; page size All shows every card with no `#pagebar`; `test_no_profile_is_byte_identical` passes unedited. Serves: O-8
- **FR-10 — Per-cell node inspector.** The Debug group gains an **Inspect cells** toggle that, under **each** requirement node card, injects a per-node data breakdown showing every field IN that node (its value) alongside HOW it is displayed — the field→element mapping (`label`→`.lbl` · `key`→`.lbl-key` · `status`→badge · `detail`→`.det` · `lives`→`.lives` · `meta`→`.node-meta` via Show node metadata · a field the card does not render → "not displayed") — so one can see, per node, what it contains, what is shown, and how; each card's real node data is stashed on its element at render so the mapping is exact (not order-guessed); additive, default off, re-applied after a re-render, profiled-navigator-only → app path byte-identical. Name: An Inspect cells toggle shows each node's data and field-to-element display mapping underneath its card. Touches: `src/startd8/navigator/view_definition.py`, `src/startd8/wireframe_view/_template.py`, `tests/unit/wireframe/test_render_profile.py`, `tests/unit/navigator/test_view_definition.py`. Lives: code src/startd8/wireframe_view/_template.py. Approve?: does each cell expose its node's data + how each field is displayed, underneath the card, additive + app-path-safe?. Verify: the Debug group includes an `inspectCells` toggle; a profiled render stashes each card's node data (`_nodeData`) and, with Inspect cells on, appends a `.node-inspect` **`<table>`** under each `.item` with the three headings **node data · value · how it's displayed** (one row per field); `test_no_profile_is_byte_identical` passes unedited. Serves: O-7
- **FR-11 — Edit not-displayed fields live in the requirements view (non-persistent).** In the Inspect-cells table, a field the card does NOT render ("not displayed") has an **editable** value cell (`contenteditable`); typing a value updates the node's in-memory data and surfaces that field as a line in the card — a **non-persistent** change that affects only the HTML view being shown (lost on reload, never written to disk), exactly like the other top-right view controls. Displayed fields' value cells stay read-only (the card owns them). Name: Not-displayed node fields are editable inline in the inspector, updating only the shown HTML view non-persistently. Touches: `src/startd8/wireframe_view/_template.py`, `tests/unit/wireframe/test_render_profile.py`. Lives: code src/startd8/wireframe_view/_template.py. Approve?: can a reader edit a not-displayed field inline and see it in the view, without persisting anything?. Verify: in a profiled render's inspector table, a not-displayed field's value cell is `contenteditable` and carries an edit hook that writes `_nodeData[field]` and injects a `[data-ni-add]` line into the card; no write path to disk exists (edit is in-memory only); the app path is byte-identical. Serves: O-7
- **FR-12 — Inline on/off switch on EVERY field to toggle its display.** In the Inspect-cells table, **every** field's row carries an inline **on/off switch**, so a reader can flip any node element on or off — crucially including ones **not currently shown**, to reveal their value in the card. Displayed fields (`label`→`.lbl` · `key`→`.lbl-key` · `status`→badge · `detail`→`.det` · `lives`→`.lives` · `meta`→`.node-meta`) default **on** and the switch hides/shows that existing element; not-displayed fields default **off** and the switch **surfaces** the field's value as a line in the card (so you see elements not otherwise rendered) — their value cell stays editable (FR-11), and editing auto-surfaces. All changes are per-card and **non-persistent** (only the shown HTML view, like the other top-right controls). Name: An inline on/off switch on every field toggles its display — hiding shown elements or surfacing the value of ones not currently shown — non-persistently. Touches: `src/startd8/wireframe_view/_template.py`, `tests/unit/wireframe/test_render_profile.py`. Lives: code src/startd8/wireframe_view/_template.py. Approve?: does every field (shown or not) have a switch, and does turning on a not-shown field reveal its value in the card, without persisting anything?. Verify: in a profiled render's inspector table, a displayed field's row has a switch (`data-ni-toggle`=element selector) that toggles that element's `display`, and a not-displayed field's row has a switch (`data-ni-show`=field) that surfaces/removes a `[data-ni-add]` line carrying the value; edits are in-memory (no disk write); the app path is byte-identical. Serves: O-7
- **FR-13 — Migrate the display-logic DATA into the View Definition.** The **field→element display mapping** (`field_display`: field → `{how, sel}`, the inspector's `INSPECT_MAP`) and the **region display-logic templates** (`region_templates`: region-id → template HTML, the frame's `FRAME_TEMPLATES`) — previously hardcoded in the template JS — become **View Definition data sections**, carried by `BASE_NAVIG8R_DEFINITION`, projected onto `RenderProfile` by `to_render_profile`, and read by the template from `payload.profile` (not hardcoded constants); a domain delta can reconfigure either via the cascade, while the *interaction* JS (switches, paging, inspector behavior) stays renderer-specific per NR-1. Additive + app-path byte-identical (the profiled payload carries the data; the app path has no profile). Name: The field-display mapping and region templates move from hardcoded JS into the View Definition, projected onto the profile and read from it. Touches: `src/startd8/navigator/view_definition.py`, `src/startd8/wireframe/profile.py`, `src/startd8/wireframe_view/_template.py`, `tests/unit/navigator/test_view_definition.py`, `tests/unit/wireframe/test_render_profile.py`. Lives: code src/startd8/navigator/view_definition.py. Approve?: does the field→element mapping + region templates live in the View Definition (projected onto the profile), not hardcoded in the template?. Verify: `resolve(BASE_NAVIG8R_DEFINITION, DEFINITION_REGISTRY).field_display["status"]["sel"] == ".badge"` and `.region_templates["outline"]` is non-empty; `to_render_profile(...).field_display`/`.region_templates` carry them; the template reads `payload.profile.field_display`/`.region_templates` (no `var INSPECT_MAP={` / `var FRAME_TEMPLATES={` literal remains); `test_no_profile_is_byte_identical` passes unedited. Serves: O-3, O-7

## Non-requirements

- **NR-1:** No control DSL. The definition declares *what* groups/toggles exist and their labels; the class-toggling **interaction JS** (including the Show-View-Definition flip and per-layer disclosure) stays renderer-specific in `_template.py` — the should-we boundary carried from REQ-14/15 NR-1. No per-toggle JavaScript is stored in the definition.
- **NR-2:** No new layers, regions, anatomy, view modes, or debug capabilities beyond promoting the existing frame to an in-render toggle and reshaping the existing controls — a reshape + expose, not new machinery.
- **NR-3:** Deliberate density-mode retirement, value preserved. The `combined` and `structOnly` *density modes* are retired by design (they answered "show structure not prose" with the wrong axis, now owned by the View Definition pick); their only unique payload — the per-node `item.meta` — is preserved as the additive **Show node metadata** overlay, and `scaffoldOnly` folds into the View Definition view. The single intentional loss is **prose-stripping-for-compactness** (bare-key skeleton of the requirement), judged not worth a standing control. Every other distinct effect (Requirement render · the frame · node metadata · region outlines · per-layer disclosure · hide app-scaffold chrome) remains reachable.
- **NR-4:** The in-render View Definition mode is **content-free by design** even though it uses a domain's definition — it renders that definition's regions/meta/control surface, never the domain's requirement nodes. It is not a new per-domain content view.

## Appendix A — Accepted (with where merged)
*(none yet — CRP incoming)*

## Appendix B — Rejected (with rationale)
*(none yet — CRP incoming)*

## Appendix C — Incoming review rounds
*(none yet)*

*v0.3 — Control axis resolved with the operator: panel collapses to one View picker (Requirement / View Definition) + one Overlays stack; the Structure/Combined density modes are retired (their `item.meta` payload preserved as a Show-node-metadata overlay), `scaffoldOnly` folded; "View Definition" uses the current renderer's resolved definition (not base); byte-identity holds for apps + node content while the navigator panel test is the one deliberate edit.*

*v0.4 — Follow-on increment (O-6/FR-6): the View Definition now shows the node display-logic — the frame's node-driven region renders a node-card field template (label/key/status/detail/lives/meta) so it meta-describes HOW a requirement value renders, not just where nodes go. Plus a frontend-design pass moving the scaffold region labels inside each region's top-left corner (blueprint room-label placement) to fix the nested/stacked-label overlap.*

*v0.5 — O-6/FR-6 generalized to ALL regions: every region (masthead ← profile chrome, glance ← computed summary, control ← audience×fluency lens, status legend ← profile.statuses[], section lead ← profile.section_lead, outline ← node card) now renders its own slot-annotated display-logic template in the frame, so no region is blank and the View Definition shows what each region derives from — not only the node region.*

*v0.6 — Richer debug mode (O-7): FR-7 makes the frame's provenance readout a definition summary instead of false "cruft" when no requirement is loaded; FR-8 adds a definition-owned **Debug** control group (Raw data · Node data toggles) that renders the raw payload + node items being visualized into a panel below the export sign-off. Control taxonomy extends to {view, overlays, debug}.*

*v0.7 — Functional capabilities through the View Definition (O-8): FR-9 adds a definition-owned **Paging** control (page size All/10/5/1) with a prev/next bar that shows N requirement nodes at a time and pages through the rest down to one at a time — the first FUNCTIONAL (not just presentational) display capability the View Definition owns. Control taxonomy extends to {view, overlays, debug, paging}.*

*v0.8 — Per-cell node inspector (O-7): FR-10 adds an **Inspect cells** Debug toggle that, under each node card, shows that node's data (every field + value) alongside HOW each field is displayed (the field→element mapping, or "not displayed") — see what a node contains, what is shown, and how. Each card's real node data is stashed on its element at render so the mapping is exact.*

*v0.9 — Inspector refinements (O-7): FR-10 renders as a `<table>` with headings node data · value · how it's displayed; FR-11 makes the "not displayed" value cells editable inline (contenteditable) — a non-persistent edit that updates the node's in-memory data and surfaces the field in the card, affecting only the shown HTML view like the other top-right controls (never written to disk).*

*v0.10 — Inline element switches (O-7): FR-12 adds an on/off switch to each displayed field's row in the inspector table, toggling that node element's display in the card (non-persistent, per-card) — flip label/status/detail/lives/meta on and off inline like the other view controls.*

*v0.11 — Every field gets a switch (O-7): FR-12 extended so ALL fields — not just currently-displayed ones — carry an on/off switch. Displayed fields toggle their element (default on); not-displayed fields default off and turning one on SURFACES its value in the card, so a reader can reveal the value of elements not otherwise shown. Non-persistent, per-card.*

*v0.12 — Display-logic data migrated into the View Definition (O-3/O-7): FR-13 moves the field→element mapping (`field_display`) and region display templates (`region_templates`) out of hardcoded template JS and into `BASE_NAVIG8R_DEFINITION`, projected onto `RenderProfile` and read from `payload.profile` — so the display-logic DATA is now definition-owned (a domain can reconfigure it), while the interaction JS stays renderer-specific per NR-1.*

*v0.13 — Frame layout fix (FR-6/REQ-15 FR-1): frame-bare now hides real region content with `display:none` (excluding the vd-template) instead of `visibility:hidden`, so hidden content takes NO space — removing the large empty region a paged-in node card's blank height used to leave. Per-layer disclosure restores via `display:revert`. Also DIDL: `_initiative_slug` strips a bare `REQ-`/`PLAN-` brand so a semantic filename's canonical initiative is clean.*
