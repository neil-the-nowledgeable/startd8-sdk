# CRP Focus — Synthetic-Probe P1-P3 (#308, v0.3.1)

**Least-reviewed:** brand-new doc, zero prior review.

## Weight on:
1. **FR-P1-4 / the lifecycle boundary (highest).** Is "P1 emits runner spec, does NOT write the SLO; promotion waits for P2 live confirmation" the right seam, or should P1 write the SLO with a pending marker? Does the `probe_runner_emitted` reason_code advance cleanly from P0's `probe_pending_no_runner`? Any way the P1 runner spec's own content leaks into PromQL replay?
2. **FR-P2-1 the `pending_probe` verdict.** Adding ONE value to the closed verdict taxonomy — does it interact correctly with the rollup (`unknown > fail > pass`), the CI-gate `fail_verdicts` set, and baseline diffing (`verdict_id`)? Could a pending_probe ever be counted as a new dead SLI (exit 2) — the exact thing it must never do? Where does compare-live learn a metric is a probe metric (fr_coverage key match vs metric-name heuristic)?
3. **FR-P1-2 secret-reference.** Is reusing the notification-policy `# UNRESOLVED REQUIRED PARAM:` discipline sound for a runner spec, or does an executable runner need stronger guarantees (no partial spec that looks runnable but has a dangling secret)?
4. **FR-P3-1 the pure delta core + P3 honesty.** Is the pure-function core genuinely testable without traces, and is the trace-gated live-validation split honest (not shipping an untested "link-aware" claim)?
5. **P2/P3 external-dependency honesty.** Are the NR/FR statements clear that P2-live and P3-validation need an external subject, so nothing is claimed "done" that isn't unit-provable?

## Settled — do NOT relitigate:
- P0 (shipped #312), #300/#307/#310, NR-1 no fabricated threshold.
- The R1-F1 "no dead-SLI on disk" principle from P0.
- The propagation_style :link cross-trace premise.
- That P2-live and P3-validation are genuinely external (do not demand a faked live proof).
